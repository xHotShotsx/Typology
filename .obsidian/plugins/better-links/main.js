/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => BetterLinksPlugin
});
module.exports = __toCommonJS(main_exports);
var import_obsidian7 = require("obsidian");

// src/i18n.ts
var ZH_CN = {
  settingsEdgeProtectionName: "\u542F\u7528\u8FB9\u754C\u4FDD\u62A4",
  settingsEdgeProtectionDesc: "\u9632\u6B62\u5728\u94FE\u63A5\u6700\u524D/\u6700\u540E\u70B9\u51FB\u65F6\u5F39\u7A97\uFF0C\u4FBF\u4E8E\u63D2\u5165\u6216\u8FFD\u52A0\u6587\u672C\u3002",
  settingsValidateInternalLinksName: "\u6821\u9A8C\u5185\u90E8\u94FE\u63A5\u6709\u6548\u6027",
  settingsValidateInternalLinksDesc: "\u4FEE\u6539\u5185\u90E8\u94FE\u63A5\u76EE\u6807\u65F6\uFF0C\u81EA\u52A8\u68C0\u6D4B\u5BF9\u5E94\u6587\u4EF6\u6216\u6807\u9898\u662F\u5426\u5B58\u5728\u3002\u82E5\u4E0D\u5B58\u5728\uFF0C\u5219\u663E\u793A\u8B66\u544A\u5E76\u963B\u6B62\u4FDD\u5B58\u3002\u53EF\u70B9\u51FB\u5F3A\u5236\u4FDD\u5B58\u6309\u94AE\u8DF3\u8FC7\u68C0\u67E5\u3002",
  noticeInternalLinkNotFound: "\u76EE\u6807\u6587\u4EF6\u6216\u6807\u9898\u4E0D\u5B58\u5728\uFF0C\u672A\u4FDD\u5B58\u3002\u70B9\u51FB \u2713 \u6309\u94AE\u53EF\u5F3A\u5236\u4FDD\u5B58\u3002\uFF08\u53EF\u5728\u8BBE\u7F6E\u4E2D\u5173\u95ED\u81EA\u52A8\u68C0\u67E5\uFF09",
  popoverAriaForceSave: "\u5F3A\u5236\u4FDD\u5B58\uFF08\u8DF3\u8FC7\u6821\u9A8C\uFF09",
  popoverWarningTargetNotFound: "\u76EE\u6807\u6587\u4EF6\u6216\u6807\u9898\u4E0D\u5B58\u5728",
  settingsEnableLinkSuggestionsName: "\u542F\u7528\u94FE\u63A5\u5EFA\u8BAE",
  settingsEnableLinkSuggestionsDesc: "\u5728\u7F16\u8F91\u5185\u90E8\u94FE\u63A5\u76EE\u6807\u65F6\uFF0C\u663E\u793A\u5E93\u5185\u7B14\u8BB0\u548C\u6807\u9898\u7684\u81EA\u52A8\u8865\u5168\u5019\u9009\u3002",
  settingsLinkSuggestionsGroup: "\u94FE\u63A5\u5EFA\u8BAE",
  settingsSyncAliasName: "\u9009\u4E2D\u65F6\u540C\u6B65\u522B\u540D",
  settingsSyncAliasDesc: "\u4ECE\u5EFA\u8BAE\u5217\u8868\u9009\u4E2D\u7B14\u8BB0\u6216\u6807\u9898\u65F6\uFF0C\u81EA\u52A8\u586B\u5165\u663E\u793A\u6587\u672C\uFF08\u522B\u540D\uFF09\u3002",
  settingsAliasSyncModeName: "\u6807\u9898\u522B\u540D\u683C\u5F0F",
  settingsAliasSyncModeDesc: "\u9009\u4E2D\u6807\u9898\u65F6\uFF0C\u751F\u6210\u7684\u522B\u540D\u683C\u5F0F\u3002",
  settingsAliasModeHeadingOnly: "\u4EC5\u6807\u9898",
  settingsAliasModeFileThenHeading: "\u6587\u4EF6\u540D > \u6807\u9898",
  settingsAliasModeHeadingThenFile: "\u6807\u9898 > \u6587\u4EF6\u540D",
  settingsAliasSeparatorName: "\u8FDE\u63A5\u7B26",
  settingsAliasSeparatorDesc: '\u5408\u5E76\u6587\u4EF6\u540D\u548C\u6807\u9898\u65F6\u4F7F\u7528\u7684\u8FDE\u63A5\u7B26\u53F7\uFF08\u9ED8\u8BA4 " > "\uFF09\u3002',
  settingsAliasTitlePropertyName: "\u6587\u4EF6\u540D\u5C5E\u6027",
  settingsAliasTitlePropertyDesc: "\u8BFB\u53D6\u6587\u4EF6\u540D\u65F6\u4F18\u5148\u4F7F\u7528\u7684 frontmatter \u5C5E\u6027\uFF08\u9ED8\u8BA4 title\uFF0C\u627E\u4E0D\u5230\u65F6\u4F7F\u7528\u6587\u4EF6\u540D\uFF09\u3002",
  commandCloseLinkEditor: "\u5173\u95ED\u94FE\u63A5\u7F16\u8F91\u5668",
  commandCreateLink: "\u63D2\u5165\u6216\u7F16\u8F91\u94FE\u63A5",
  noticeLinkUpdated: "\u94FE\u63A5\u5DF2\u66F4\u65B0\u3002",
  noticeLinkRemoved: "\u94FE\u63A5\u5DF2\u5220\u9664\u3002",
  noticeLinkRemovedAll: "\u94FE\u63A5\u53CA\u6587\u672C\u5DF2\u5168\u90E8\u5220\u9664\u3002",
  noticeNoActiveEditor: "\u672A\u627E\u5230\u5F53\u524D\u7F16\u8F91\u5668\u3002",
  noticeEmptyDestination: "\u94FE\u63A5\u76EE\u6807\u4E3A\u7A7A\u3002",
  noticeCopiedMarkdown: "\u5DF2\u590D\u5236 Markdown \u94FE\u63A5\u3002",
  noticeCopiedUrl: "\u5DF2\u590D\u5236 URL\u3002",
  noticeCopiedFileName: "\u5DF2\u590D\u5236\u6587\u4EF6\u540D\u3002",
  noticeNothingToCopy: "\u6CA1\u6709\u53EF\u590D\u5236\u7684\u5185\u5BB9\u3002",
  noticeCopyFailed: "\u590D\u5236\u5931\u8D25\u3002",
  noticeClickedLinkMissing: "\u88AB\u70B9\u51FB\u7684\u94FE\u63A5\u5DF2\u4E0D\u53EF\u7528\u3002",
  settingsHeading: "\u94FE\u63A5\u7F16\u8F91\u5668",
  settingsGeneral: "\u5E38\u89C4",
  settingsEnableEditorName: "\u542F\u7528\u94FE\u63A5\u7F16\u8F91\u5668",
  settingsEnableEditorDesc: "\u5728 Markdown \u7F16\u8F91\u5668\u4E2D\u62E6\u622A\u652F\u6301\u7684\u94FE\u63A5\u5E76\u6253\u5F00\u6D6E\u52A8\u7F16\u8F91\u5668\u3002",
  settingsSupportedTypes: "\u652F\u6301\u7684\u94FE\u63A5\u7C7B\u578B",
  settingsWikiName: "Wikilink",
  settingsWikiDesc: "\u652F\u6301 [[note]] \u548C [[note|alias]]\u3002",
  settingsMarkdownName: "Markdown \u94FE\u63A5",
  settingsMarkdownDesc: "\u652F\u6301 [label](target)\u3002",
  settingsUrlName: "\u7EAF\u7F51\u9875\u94FE\u63A5",
  settingsUrlDesc: "\u652F\u6301 https://example.com \u8FD9\u7C7B\u88F8\u94FE\u63A5\u3002",
  settingsImageName: "\u56FE\u7247\u94FE\u63A5",
  settingsImageDesc: "\u652F\u6301\u56FE\u7247\u683C\u5F0F\u94FE\u63A5\uFF0C\u5982 ![[image.png|100x80]]\u3001![100x80](image.png) \u4EE5\u53CA [100x80](image.png)\u3002",
  settingsActions: "\u52A8\u4F5C",
  settingsOpenExternalName: "\u5916\u90E8\u94FE\u63A5\u6253\u5F00\u65B9\u5F0F",
  settingsOpenExternalDesc: "\u9009\u62E9\u5728\u7CFB\u7EDF\u6D4F\u89C8\u5668\u4E2D\u6253\u5F00\uFF0C\u6216\u5728 Obsidian \u7A97\u53E3\u4E2D\u6253\u5F00\u3002",
  settingsOpenExternalBrowser: "\u7CFB\u7EDF\u6D4F\u89C8\u5668",
  settingsOpenExternalObsidian: "Obsidian \u7A97\u53E3",
  settingsOpenInternalName: "\u5185\u90E8\u94FE\u63A5\u6253\u5F00\u65B9\u5F0F",
  settingsOpenInternalDesc: "\u901A\u8FC7\u6D6E\u7A97\u300C\u6253\u5F00\u94FE\u63A5\u300D\u6309\u94AE\u6253\u5F00\u5185\u90E8\u94FE\u63A5\u65F6\uFF0C\u5728\u54EA\u91CC\u6253\u5F00\u3002",
  settingsOpenInternalCurrent: "\u5F53\u524D\u6807\u7B7E\u9875",
  settingsOpenInternalTab: "\u65B0\u6807\u7B7E\u9875",
  settingsOpenInternalWindow: "\u65B0\u7A97\u53E3",
  settingsOpenInternalSplitH: "\u5DE6\u53F3\u5206\u5C4F",
  settingsOpenInternalSplitV: "\u4E0A\u4E0B\u5206\u5C4F",
  settingsSmartSplitName: "\u667A\u80FD\u5206\u5C4F",
  settingsSmartSplitDesc: "\u5DF2\u6709\u540C\u65B9\u5411\u5206\u5C4F\u65F6\uFF0C\u590D\u7528\u53E6\u4E00\u4E2A\u9762\u677F\u800C\u975E\u65B0\u5EFA\u5206\u5C4F\u3002",
  settingsDeleteBehaviorName: "\u5220\u9664\u94FE\u63A5\u884C\u4E3A",
  settingsDeleteBehaviorDesc: "\u9009\u62E9\u5220\u9664\u94FE\u63A5\u65F6\u4FDD\u7559\u53EF\u89C1\u6587\u672C\uFF0C\u6216\u5168\u90E8\u79FB\u9664\u3002\u6309\u4F4F Ctrl \u70B9\u51FB\u5220\u9664\u6309\u94AE\uFF0C\u53EF\u5F3A\u5236\u5220\u9664\u6574\u4E2A\u94FE\u63A5\u3002",
  settingsDeletePreserve: "\u4FDD\u7559\u6587\u672C",
  settingsDeleteRemoveAll: "\u5168\u90E8\u79FB\u9664",
  settingsShowEmbedToggleName: "\u663E\u793A\u5D4C\u5165\u5207\u6362\u6309\u94AE",
  settingsShowEmbedToggleDesc: "\u542F\u7528\u540E\uFF0C\u5728\u6D6E\u7A97\u300C\u6253\u5F00\u94FE\u63A5\u300D\u6309\u94AE\u65C1\u663E\u793A\u5D4C\u5165\u5207\u6362\u6309\u94AE\uFF0C\u7528\u4E8E\u5FEB\u901F\u5207\u6362\u94FE\u63A5\u662F\u5426\u5E26 ! \u524D\u7F00\u3002",
  popoverPlaceholderDisplay: "\u663E\u793A\u6587\u672C",
  popoverPlaceholderImageSize: "\u56FE\u7247\u5C3A\u5BF8\uFF0C\u4F8B\u5982 100x80",
  popoverPlaceholderDestination: "URL \u6216\u7B14\u8BB0\u8DEF\u5F84",
  popoverAriaOpen: "\u6253\u5F00\u94FE\u63A5",
  popoverHintCtrlClick: "Ctrl+\u70B9\u51FB",
  popoverAriaCopyMarkdown: "\u590D\u5236\u4E3A Markdown",
  popoverAriaCopyWikiLink: "\u590D\u5236\u4E3A Wikilink",
  popoverAriaCopyUrl: "\u590D\u5236 URL",
  popoverAriaCopyFileName: "\u590D\u5236\u6587\u4EF6\u540D",
  popoverAriaDelete: "\u5220\u9664\u94FE\u63A5",
  popoverAriaToggleEmbed: "\u5207\u6362\u5D4C\u5165",
  popoverAriaEmbedOn: "\u5F53\u524D\uFF1A\u5DF2\u5D4C\u5165\uFF08\u70B9\u51FB\u53D6\u6D88\uFF09",
  popoverAriaEmbedOff: "\u5F53\u524D\uFF1A\u672A\u5D4C\u5165\uFF08\u70B9\u51FB\u5D4C\u5165\uFF09",
  typeLabelWiki: "Wikilink",
  typeLabelImageWiki: "Wikilink \u56FE\u7247",
  typeLabelMarkdown: "Markdown",
  typeLabelImageMarkdown: "Markdown \u56FE\u7247",
  typeLabelUrl: "URL",
  settingsTriggerMethodName: "\u89E6\u53D1\u65B9\u5F0F",
  settingsTriggerMethodDesc: "\u9009\u62E9\u6253\u5F00\u7F16\u8F91\u6D6E\u7A97\u7684\u4EA4\u4E92\u65B9\u5F0F\u3002\u300C\u60AC\u6D6E\u300D\u65F6\u9F20\u6807\u505C\u7559\u5728\u94FE\u63A5\u4E0A\u5373\u5F39\u51FA\u7F16\u8F91\u7A97\u53E3\uFF1B\u300C\u70B9\u51FB\u300D\u65F6\u9700\u5DE6\u952E\u70B9\u51FB\u94FE\u63A5\uFF1B\u300C\u53F3\u952E\u300D\u65F6\u9700\u53F3\u952E\u70B9\u51FB\u94FE\u63A5\u3002",
  settingsTriggerMethodHover: "\u60AC\u6D6E",
  settingsTriggerMethodClick: "\u70B9\u51FB",
  settingsTriggerMethodRightClick: "\u53F3\u952E",
  settingsTriggerModifierName: "\u89E6\u53D1\u4FEE\u9970\u952E",
  settingsTriggerModifierDesc: "\u9009\u4E2D\u4FEE\u9970\u952E\u540E\uFF0C\u5FC5\u987B\u6309\u4F4F\u5BF9\u5E94\u6309\u952E\u624D\u4F1A\u89E6\u53D1\u7F16\u8F91\u6D6E\u7A97\uFF1B\u672A\u6309\u4F4F\u65F6\u4FDD\u6301\u9ED8\u8BA4\u884C\u4E3A\u3002",
  settingsTriggerModifierNone: "\u65E0\u4FEE\u9970\u952E",
  settingsTriggerModifierCtrl: "Ctrl",
  settingsTriggerModifierShift: "Shift",
  settingsTriggerModifierAlt: "Alt",
  settingsDisableNativeClickName: "\u7981\u7528\u5DE6\u952E\u539F\u751F\u70B9\u51FB",
  settingsDisableNativeClickDesc: "\u542F\u7528\u540E\uFF0C\u5DE6\u952E\u70B9\u51FB\u94FE\u63A5\u4E0D\u518D\u89E6\u53D1\u8DF3\u8F6C\uFF0C\u4EC5\u79FB\u52A8\u5149\u6807\u3002Ctrl+Click \u7B49\u5E26\u4FEE\u9970\u952E\u7684\u70B9\u51FB\u4E0D\u53D7\u5F71\u54CD\u3002\u4EC5\u5728\u89E6\u53D1\u65B9\u5F0F\u975E\u300C\u70B9\u51FB\u300D\u65F6\u53EF\u7528\u3002",
  settingsAlwaysShowDisplayTextName: "\u603B\u662F\u663E\u793A DisplayText",
  settingsAlwaysShowDisplayTextDesc: "\u5173\u95ED\u65F6\uFF0C\u6CA1\u6709\u663E\u5F0F\u8BBE\u7F6E\u663E\u793A\u6587\u672C\u7684\u94FE\u63A5\uFF08\u5982\u7EAF URL\u3001[[\u7B14\u8BB0\u540D]]\uFF09\u5728\u7F16\u8F91\u7A97\u4E2D DisplayText \u8F93\u5165\u6846\u5C06\u4FDD\u6301\u7A7A\u767D\u3002",
  settingsPadNewLinkWithSpacesName: "\u65B0\u5EFA\u94FE\u63A5\u4E24\u4FA7\u81EA\u52A8\u52A0\u7A7A\u683C",
  settingsPadNewLinkWithSpacesDesc: "\u901A\u8FC7\u547D\u4EE4\u5728\u5149\u6807\u5904\u63D2\u5165\u65B0\u94FE\u63A5\u65F6\uFF0C\u82E5\u4E24\u4FA7\u4E0D\u662F\u7A7A\u767D/\u6807\u70B9\uFF0C\u81EA\u52A8\u8865\u4E00\u4E2A\u7A7A\u683C\u3002\u5149\u6807\u4F1A\u843D\u5728\u53F3\u4FA7\u7A7A\u683C\u4E4B\u540E\uFF0C\u65B9\u4FBF\u7EE7\u7EED\u8F93\u5165\u3002",
  settingsExcludeGroup: "\u6392\u9664\u7279\u5B9A\u94FE\u63A5",
  settingsExcludeModeName: "\u4F55\u65F6\u6392\u9664",
  settingsExcludeModeDesc: "\u5F53\u94FE\u63A5\u76EE\u6807\u5305\u542B\u6392\u9664\u5173\u952E\u5B57\u65F6\uFF0C\u5728\u6240\u9009\u7684\u89E6\u53D1\u573A\u666F\u4E0B\u8DF3\u8FC7\u7F16\u8F91\u6D6E\u7A97\u3002",
  settingsExcludeModeDisabled: "\u7981\u7528",
  settingsExcludeModeHover: "\u60AC\u6D6E",
  settingsExcludeModeClick: "\u70B9\u51FB",
  settingsExcludeModeAll: "\u5168\u90E8",
  settingsExcludeKeywordsName: "\u6392\u9664\u5173\u952E\u5B57",
  settingsExcludeKeywordsDesc: "\u94FE\u63A5\u76EE\u6807\u5305\u542B\u4EE5\u4E0B\u4EFB\u4E00\u5173\u952E\u5B57\u65F6\uFF0C\u4E0D\u5F39\u51FA\u7F16\u8F91\u6D6E\u7A97\u3002\u7528\u9017\u53F7\u6216\u6362\u884C\u5206\u9694\u3002",
  settingsDebugGroup: "\u8C03\u8BD5",
  settingsDebugModeName: "\u8C03\u8BD5\u6A21\u5F0F",
  settingsDebugModeDesc: "\u5F00\u542F\u540E\u5728\u5F00\u53D1\u8005\u63A7\u5236\u53F0\u8F93\u51FA\u8BE6\u7EC6\u65E5\u5FD7\uFF0C\u7528\u4E8E\u6392\u67E5\u95EE\u9898\uFF08\u5982\u79FB\u52A8\u7AEF\u70B9\u51FB\u94FE\u63A5\u4E0D\u751F\u6548\uFF09\u3002\u6B63\u5E38\u4F7F\u7528\u65F6\u8BF7\u5173\u95ED\u3002"
};
var EN_US = {
  settingsEdgeProtectionName: "Enable boundary protection",
  settingsEdgeProtectionDesc: "Prevent popup when clicking at the very start or end of a link, so you can insert or append text easily.",
  settingsValidateInternalLinksName: "Validate internal links",
  settingsValidateInternalLinksDesc: "When editing an internal link target, check whether the target file or heading exists. If not, show a warning and block saving. Click the \u2713 button to force save.",
  noticeInternalLinkNotFound: "Target file or heading not found. Changes not saved. Click the \u2713 button to force save. (You can disable auto-validation in settings)",
  popoverAriaForceSave: "Force save (skip validation)",
  popoverWarningTargetNotFound: "Target file or heading not found",
  settingsEnableLinkSuggestionsName: "Enable link suggestions",
  settingsEnableLinkSuggestionsDesc: "Show autocomplete suggestions for notes and headings when editing internal link destinations.",
  settingsLinkSuggestionsGroup: "Link suggestions",
  settingsSyncAliasName: "Sync alias on selection",
  settingsSyncAliasDesc: "Automatically fill in the display text (alias) when selecting a note or heading from the suggestion list.",
  settingsAliasSyncModeName: "Heading alias format",
  settingsAliasSyncModeDesc: "The alias format to use when selecting a heading.",
  settingsAliasModeHeadingOnly: "Heading only",
  settingsAliasModeFileThenHeading: "Filename > Heading",
  settingsAliasModeHeadingThenFile: "Heading > Filename",
  settingsAliasSeparatorName: "Separator",
  settingsAliasSeparatorDesc: 'Separator used when combining filename and heading (default: " > ").',
  settingsAliasTitlePropertyName: "Title property",
  settingsAliasTitlePropertyDesc: "Frontmatter property to use as the filename display name (default: title, falls back to file basename if not found).",
  commandCloseLinkEditor: "Close link editor",
  commandCreateLink: "Insert or edit link at cursor",
  noticeLinkUpdated: "Link updated.",
  noticeLinkRemoved: "Link removed.",
  noticeLinkRemovedAll: "Link and text removed.",
  noticeNoActiveEditor: "No active editor was found.",
  noticeEmptyDestination: "Link destination is empty.",
  noticeCopiedMarkdown: "Copied Markdown link.",
  noticeCopiedUrl: "Copied URL.",
  noticeCopiedFileName: "Copied file name.",
  noticeNothingToCopy: "Nothing to copy.",
  noticeCopyFailed: "Copy failed.",
  noticeClickedLinkMissing: "The clicked link is no longer available.",
  settingsHeading: "Link editor",
  settingsGeneral: "General",
  settingsEnableEditorName: "Enable link editor",
  settingsEnableEditorDesc: "Intercept supported links in the Markdown editor and open the floating editor.",
  settingsSupportedTypes: "Supported link types",
  settingsWikiName: "Wikilinks",
  settingsWikiDesc: "Support links like [[note]] and [[note|alias]].",
  settingsMarkdownName: "Markdown links",
  settingsMarkdownDesc: "Support links like [label](target).",
  settingsUrlName: "Plain web links",
  settingsUrlDesc: "Support plain links like https://example.com.",
  settingsImageName: "Image links",
  settingsImageDesc: "Support image-form links such as ![[image.png|100x80]], ![100x80](image.png), and [100x80](image.png).",
  settingsActions: "Actions",
  settingsOpenExternalName: "Open external links in",
  settingsOpenExternalDesc: "Choose whether external web links open in the system browser or in an Obsidian window.",
  settingsOpenExternalBrowser: "System browser",
  settingsOpenExternalObsidian: "Obsidian window",
  settingsOpenInternalName: "Open internal links in",
  settingsOpenInternalDesc: 'Where to open internal links when using the "Open link" button in the popover.',
  settingsOpenInternalCurrent: "Current tab",
  settingsOpenInternalTab: "New tab",
  settingsOpenInternalWindow: "New window",
  settingsOpenInternalSplitH: "Split left & right",
  settingsOpenInternalSplitV: "Split top & bottom",
  settingsSmartSplitName: "Smart split",
  settingsSmartSplitDesc: "When a split in the same direction already exists, reuse the other pane instead of creating a new split.",
  settingsDeleteBehaviorName: "Delete link behavior",
  settingsDeleteBehaviorDesc: "Choose whether deleting a link keeps its visible text or removes it completely. Hold Ctrl while clicking the delete button to always remove everything regardless of this setting.",
  settingsDeletePreserve: "Preserve text",
  settingsDeleteRemoveAll: "Remove everything",
  settingsShowEmbedToggleName: "Show embed toggle button",
  settingsShowEmbedToggleDesc: "When enabled, an embed toggle button appears in the popover. Use it to quickly add or remove the ! prefix.",
  popoverPlaceholderDisplay: "Display text",
  popoverPlaceholderImageSize: "Image size, e.g. 100x80",
  popoverPlaceholderDestination: "URL or note path",
  popoverAriaOpen: "Open link",
  popoverHintCtrlClick: "Ctrl+Click",
  popoverAriaCopyMarkdown: "Copy as Markdown",
  popoverAriaCopyWikiLink: "Copy as WikiLink",
  popoverAriaCopyUrl: "Copy URL",
  popoverAriaCopyFileName: "Copy file name",
  popoverAriaDelete: "Delete link",
  popoverAriaToggleEmbed: "Toggle embed",
  popoverAriaEmbedOn: "Currently embedded \u2014 click to remove",
  popoverAriaEmbedOff: "Not embedded \u2014 click to embed",
  typeLabelWiki: "WikiLink",
  typeLabelImageWiki: "WikiLink image",
  typeLabelMarkdown: "Markdown",
  typeLabelImageMarkdown: "Markdown image",
  typeLabelUrl: "URL",
  settingsTriggerMethodName: "Trigger method",
  settingsTriggerMethodDesc: "Choose how to trigger the editing popover. With Hover, the popover opens when your cursor rests on a link. With Click, you must left-click a link. With Right click, you must right-click a link.",
  settingsTriggerMethodHover: "Hover",
  settingsTriggerMethodClick: "Click",
  settingsTriggerMethodRightClick: "Right click",
  settingsTriggerModifierName: "Trigger modifier key",
  settingsTriggerModifierDesc: "When a modifier key is selected, you must hold it while hovering/clicking to trigger the editing popover; otherwise the default behavior is preserved.",
  settingsTriggerModifierNone: "None",
  settingsTriggerModifierCtrl: "Ctrl",
  settingsTriggerModifierShift: "Shift",
  settingsTriggerModifierAlt: "Alt",
  settingsDisableNativeClickName: "Disable native click",
  settingsDisableNativeClickDesc: "When enabled, left-clicking a link no longer opens it \u2014 only moves the cursor. Ctrl+Click and other modified clicks still work. Only available when the trigger method is not Click.",
  settingsAlwaysShowDisplayTextName: "Always show display text",
  settingsAlwaysShowDisplayTextDesc: "When off, links without explicit display text (e.g. plain URLs, [[notename]]) will show an empty display text field in the editor.",
  settingsPadNewLinkWithSpacesName: "Pad new links with spaces",
  settingsPadNewLinkWithSpacesDesc: "When inserting a new link at the cursor via command, automatically add a space on either side if there isn't already whitespace/punctuation. The cursor lands after the trailing space so you can keep typing.",
  settingsExcludeGroup: "Exclude specific links",
  settingsExcludeModeName: "When to exclude",
  settingsExcludeModeDesc: "Skip the editing popover when the link destination contains any exclude keyword, under the selected trigger scenario.",
  settingsExcludeModeDisabled: "Disabled",
  settingsExcludeModeHover: "Hover",
  settingsExcludeModeClick: "Click",
  settingsExcludeModeAll: "All",
  settingsExcludeKeywordsName: "Exclude keywords",
  settingsExcludeKeywordsDesc: "The editing popover will not appear when the link destination contains any of these keywords. Separate with commas or newlines.",
  settingsDebugGroup: "Debug",
  settingsDebugModeName: "Debug mode",
  settingsDebugModeDesc: "When enabled, detailed logs are printed to the developer console. Useful for diagnosing issues (e.g. links not responding on mobile). Turn off during normal use."
};
function createTranslator(localeHint) {
  const normalized = localeHint.toLowerCase();
  const messages = normalized.startsWith("zh") ? ZH_CN : EN_US;
  return (key) => {
    var _a;
    return (_a = messages[key]) != null ? _a : EN_US[key];
  };
}

// src/linkEditManager.ts
var import_obsidian4 = require("obsidian");
var import_view = require("@codemirror/view");

// src/linkActions.ts
var import_obsidian = require("obsidian");

// src/linkDetector.ts
var WIKILINK_PATTERN = /!?\[\[([^\]\r\n]+?)\]\]/g;
var MARKDOWN_LINK_PATTERN = /!?\[([^\]\r\n]*)\]\(([^)\r\n]+)\)/g;
var URL_PATTERN = /\bhttps?:\/\/[^\s<>"]+[^\s<>"),.;:!?]/g;
function findLinkAtOffset(lineText, offset2, settings, inclusiveEnd = false) {
  var _a;
  const matches = collectMatches(lineText, settings);
  return (_a = matches.find((match) => offset2 >= match.start && (inclusiveEnd ? offset2 <= match.end : offset2 < match.end))) != null ? _a : null;
}
function findLinkByDestination(lineText, destination, settings) {
  var _a;
  const matches = collectMatches(lineText, settings);
  return (_a = matches.find((m) => m.destination === destination)) != null ? _a : null;
}
function withEditorRange(match, line, sourcePath) {
  return {
    ...match,
    range: {
      from: { line, ch: match.start },
      to: { line, ch: match.end }
    },
    sourcePath
  };
}
function serializeEditedLink(match, displayText, destination, options) {
  const nextDisplayText = displayText.trim();
  const nextDestination = destination.trim();
  const wasExternal = isLikelyExternalDestination(match.destination);
  const nowInternal = !isLikelyExternalDestination(nextDestination) && nextDestination.length > 0;
  if (wasExternal && nowInternal && (options == null ? void 0 : options.preferWikiLink)) {
    const shouldIncludeAlias = nextDisplayText.length > 0 && nextDisplayText !== prettifyWikiTarget(nextDestination);
    return shouldIncludeAlias ? `[[${nextDestination}|${nextDisplayText}]]` : `[[${nextDestination}]]`;
  }
  if (match.type === "imageWiki") {
    if (!nextDestination) {
      return "";
    }
    const useEmbedSyntax = match.originalText.startsWith("![[");
    const prefix = useEmbedSyntax ? "!" : "";
    return nextDisplayText.length > 0 ? `${prefix}[[${nextDestination}|${nextDisplayText}]]` : `${prefix}[[${nextDestination}]]`;
  }
  if (match.type === "imageMarkdown") {
    const useEmbedSyntax = match.originalText.startsWith("![");
    const prefix = useEmbedSyntax ? "!" : "";
    return `${prefix}[${nextDisplayText}](${nextDestination})`;
  }
  if (match.type === "wiki") {
    if (!nextDestination) {
      return "";
    }
    const useEmbedSyntax = match.originalText.startsWith("![[");
    const prefix = useEmbedSyntax ? "!" : "";
    const shouldIncludeAlias = nextDisplayText.length > 0 && nextDisplayText !== prettifyWikiTarget(nextDestination);
    return shouldIncludeAlias ? `${prefix}[[${nextDestination}|${nextDisplayText}]]` : `${prefix}[[${nextDestination}]]`;
  }
  if (match.type === "markdown") {
    const useEmbedSyntax = match.originalText.startsWith("![");
    const prefix = useEmbedSyntax ? "!" : "";
    if (!useEmbedSyntax && nextDisplayText.length === 0) {
      return nextDestination;
    }
    return `${prefix}[${nextDisplayText}](${nextDestination})`;
  }
  if (nextDisplayText.length > 0 && nextDisplayText !== nextDestination) {
    return `[${nextDisplayText}](${nextDestination})`;
  }
  return nextDestination;
}
function toMarkdownSnippet(match, displayText, destination) {
  const nextDisplayText = (displayText.trim() || defaultDisplayText(match)).trim();
  const nextDestination = destination.trim();
  if (match.type === "imageWiki" || match.type === "imageMarkdown") {
    const useEmbedSyntax = match.type === "imageWiki" && match.originalText.startsWith("![[") || match.type === "imageMarkdown" && match.originalText.startsWith("![");
    const prefix = useEmbedSyntax ? "!" : "";
    return `${prefix}[${nextDisplayText}](${nextDestination})`;
  }
  return `[${nextDisplayText}](${nextDestination})`;
}
function deletionReplacement(match, preserveText) {
  if (!preserveText) {
    return "";
  }
  return defaultDisplayText(match);
}
function defaultDisplayText(match) {
  if (match.type === "imageWiki" || match.type === "imageMarkdown") {
    return match.displayText;
  }
  if (match.type === "wiki") {
    if (match.hasExplicitDisplayText && match.displayText.trim().length > 0) {
      return match.displayText;
    }
    return prettifyWikiTarget(match.destination);
  }
  if (match.type === "markdown") {
    return match.displayText || match.destination;
  }
  return match.destination;
}
function isLikelyExternalDestination(destination) {
  const trimmed = destination.trim();
  return /^[a-z][a-z\d+\-.]+:/i.test(trimmed) && !/^[a-z]:[/\\]/i.test(trimmed);
}
function isLikelyInternalDestination(destination) {
  const value = destination.trim();
  if (!value) {
    return false;
  }
  if (isLikelyExternalDestination(value)) {
    return false;
  }
  return true;
}
function collectMatches(lineText, settings) {
  var _a, _b, _c, _d, _e, _f;
  const matches = [];
  const inlineCodeRanges = collectInlineCodeRanges(lineText);
  if (settings.enableWikiLinks) {
    for (const match of lineText.matchAll(WIKILINK_PATTERN)) {
      const start2 = (_a = match.index) != null ? _a : 0;
      const originalText = match[0];
      const inside = (_b = match[1]) != null ? _b : "";
      const separatorIndex = inside.indexOf("|");
      const destination = separatorIndex >= 0 ? inside.slice(0, separatorIndex).trim() : inside.trim();
      const isImageDestination = isImageDestinationPath(destination);
      const isImage = isImageDestination;
      if (isImage && !settings.enableImages) {
        continue;
      }
      const displayText = isImage ? separatorIndex >= 0 ? inside.slice(separatorIndex + 1).trim() : "" : separatorIndex >= 0 ? inside.slice(separatorIndex + 1).trim() : prettifyWikiTarget(destination);
      matches.push({
        type: isImage ? "imageWiki" : "wiki",
        start: start2,
        end: start2 + originalText.length,
        originalText,
        displayText,
        destination,
        hasExplicitDisplayText: separatorIndex >= 0
      });
    }
  }
  if (settings.enableMarkdownLinks) {
    for (const match of lineText.matchAll(MARKDOWN_LINK_PATTERN)) {
      const start2 = (_c = match.index) != null ? _c : 0;
      const originalText = match[0];
      const destination = ((_d = match[2]) != null ? _d : "").trim();
      const isImageDestination = isImageDestinationPath(destination);
      const isImage = isImageDestination;
      if (isImage && !settings.enableImages) {
        continue;
      }
      matches.push({
        type: isImage ? "imageMarkdown" : "markdown",
        start: start2,
        end: start2 + originalText.length,
        originalText,
        displayText: (_e = match[1]) != null ? _e : "",
        destination,
        hasExplicitDisplayText: true
      });
    }
  }
  if (settings.enablePlainUrls) {
    for (const match of lineText.matchAll(URL_PATTERN)) {
      const start2 = (_f = match.index) != null ? _f : 0;
      const originalText = match[0];
      if (matches.some((existing) => rangesOverlap(existing.start, existing.end, start2, start2 + originalText.length))) {
        continue;
      }
      if (isInsideInlineCode(start2, start2 + originalText.length, inlineCodeRanges)) {
        continue;
      }
      matches.push({
        type: "url",
        start: start2,
        end: start2 + originalText.length,
        originalText,
        displayText: originalText,
        destination: originalText,
        hasExplicitDisplayText: false
      });
    }
  }
  return matches.sort((left2, right2) => left2.start - right2.start);
}
function prettifyWikiTarget(destination) {
  var _a, _b;
  const withoutSubpath = (_a = destination.split("#", 1)[0]) != null ? _a : destination;
  const lastSegment = (_b = withoutSubpath.split("/").pop()) != null ? _b : withoutSubpath;
  return lastSegment.replace(/\.md$/i, "");
}
function rangesOverlap(startA, endA, startB, endB) {
  return startA < endB && startB < endA;
}
function isImageDestinationPath(destination) {
  var _a;
  const cleaned = (_a = destination.split(/[?#]/, 1)[0]) != null ? _a : destination;
  return /\.(png|jpe?g|gif|webp|bmp|svg|avif|ico)$/i.test(cleaned.trim());
}
function collectInlineCodeRanges(lineText) {
  const ranges = [];
  const pattern = /`+/g;
  let openMatch = null;
  while (true) {
    const tick = pattern.exec(lineText);
    if (!tick) break;
    if (!openMatch) {
      openMatch = tick;
    } else if (tick[0].length === openMatch[0].length) {
      ranges.push([openMatch.index, tick.index + tick[0].length]);
      openMatch = null;
    }
  }
  return ranges;
}
function isInsideInlineCode(start2, end2, ranges) {
  return ranges.some(([codeStart, codeEnd]) => start2 >= codeStart && end2 <= codeEnd);
}

// src/linkActions.ts
var t = createTranslator(
  (0, import_obsidian.getLanguage)()
);
async function openLink(app, match, values, settings) {
  var _a;
  const destination = values.destination.trim();
  if (!destination) {
    new import_obsidian.Notice(t("noticeEmptyDestination"));
    return;
  }
  if (match.type === "wiki" || isLikelyInternalDestination(destination)) {
    const mode = (_a = settings.internalLinkOpenMode) != null ? _a : "tab";
    if (mode === "current") {
      await app.workspace.openLinkText(destination, match.sourcePath, false);
    } else if (mode === "tab") {
      await app.workspace.openLinkText(destination, match.sourcePath, "tab");
    } else {
      const leaf = getInternalLinkLeaf(app, settings, mode);
      await openInternalInLeaf(app, leaf, destination, match.sourcePath);
    }
    return;
  }
  if (isNonHttpExternalDestination(destination)) {
    if (await openInSystemBrowser(destination)) {
      return;
    }
    return;
  }
  if (settings.externalLinkOpenMode === "browser") {
    if (await openInSystemBrowser(destination)) {
      return;
    }
  }
  window.open(destination, "_blank", "noopener,noreferrer");
}
async function copyMarkdown(app, match, values) {
  const destination = values.destination.trim();
  const snippet = isLikelyExternalDestination(destination) ? toMarkdownSnippet(match, values.displayText, destination) : toPreferredInternalSnippet(app, match, values);
  await copyText(snippet, t("noticeCopiedMarkdown"));
}
async function copyUrl(app, match, url) {
  if (isImageType(match)) {
    const fileName = extractFileName(url);
    await copyText(fileName, t("noticeCopiedFileName"));
    return;
  }
  await copyText(url.trim(), t("noticeCopiedUrl"));
}
function buildDeletionText(match, settings) {
  return deletionReplacement(match, settings.deleteLinkBehavior === "preserve-text");
}
function normalizeEditableValues(match, displayText, destination) {
  return {
    displayText: displayText.trim() || defaultDisplayText(match),
    destination: destination.trim()
  };
}
async function openInSystemBrowser(url) {
  var _a, _b, _c;
  if (!isLikelyExternalDestination(url)) {
    return false;
  }
  try {
    const electronModule = getElectronModule();
    await ((_b = (_a = electronModule == null ? void 0 : electronModule.shell) == null ? void 0 : _a.openExternal) == null ? void 0 : _b.call(_a, url));
    return Boolean((_c = electronModule == null ? void 0 : electronModule.shell) == null ? void 0 : _c.openExternal);
  } catch (e) {
    return false;
  }
}
function isNonHttpExternalDestination(destination) {
  const trimmed = destination.trim();
  return isLikelyExternalDestination(trimmed) && !/^https?:/i.test(trimmed);
}
async function copyText(value, successMessage) {
  if (!value) {
    new import_obsidian.Notice(t("noticeNothingToCopy"));
    return;
  }
  try {
    await navigator.clipboard.writeText(value);
    new import_obsidian.Notice(successMessage);
    return;
  } catch (e) {
    const electronModule = getElectronModule();
    if (electronModule == null ? void 0 : electronModule.clipboard) {
      electronModule.clipboard.writeText(value);
      new import_obsidian.Notice(successMessage);
      return;
    }
  }
  new import_obsidian.Notice(t("noticeCopyFailed"));
}
function getElectronModule() {
  var _a;
  const scopedWindow = window;
  const loaded = (_a = scopedWindow.require) == null ? void 0 : _a.call(scopedWindow, "electron");
  if (!loaded || typeof loaded !== "object") {
    return null;
  }
  return loaded;
}
function isImageType(match) {
  return match.type === "imageWiki" || match.type === "imageMarkdown";
}
function toPreferredInternalSnippet(app, match, values) {
  if (isImageType(match)) {
    return toPreferredImageSnippet(app, values);
  }
  const destination = values.destination.trim();
  const displayText = values.displayText.trim();
  if (shouldUseWikiLinkFormat(app)) {
    if (displayText.length === 0) {
      return `[[${destination}]]`;
    }
    return `[[${destination}|${displayText}]]`;
  }
  return `[${displayText}](${destination})`;
}
function toPreferredImageSnippet(app, values) {
  const destination = values.destination.trim();
  const sizeText = values.displayText.trim();
  if (shouldUseWikiLinkFormat(app)) {
    return sizeText.length > 0 ? `![[${destination}|${sizeText}]]` : `![[${destination}]]`;
  }
  return `![${sizeText}](${destination})`;
}
function shouldUseWikiLinkFormat(app) {
  var _a, _b;
  const vaultWithConfig = app.vault;
  const useMarkdownLinks = (_a = vaultWithConfig.getConfig) == null ? void 0 : _a.call(vaultWithConfig, "useMarkdownLinks");
  if (typeof useMarkdownLinks === "boolean") {
    return !useMarkdownLinks;
  }
  const useWikiLinks = (_b = vaultWithConfig.getConfig) == null ? void 0 : _b.call(vaultWithConfig, "useWikiLinks");
  if (typeof useWikiLinks === "boolean") {
    return useWikiLinks;
  }
  return false;
}
function extractFileName(destination) {
  var _a, _b;
  const cleaned = (_a = destination.trim().split(/[?#]/, 1)[0]) != null ? _a : destination.trim();
  if (!cleaned) {
    return "";
  }
  const normalized = cleaned.replace(/\\/g, "/");
  const parts = normalized.split("/");
  return (_b = parts[parts.length - 1]) != null ? _b : cleaned;
}
async function openInternalInLeaf(app, leaf, destination, sourcePath) {
  const hashIndex = destination.indexOf("#");
  const linkpath = hashIndex >= 0 ? destination.slice(0, hashIndex) : destination;
  const subpath = hashIndex >= 0 ? destination.slice(hashIndex) : void 0;
  const file = app.metadataCache.getFirstLinkpathDest(linkpath, sourcePath);
  if (file) {
    await leaf.openFile(file, {
      active: true,
      ...subpath ? { eState: { subpath } } : {}
    });
  } else {
    app.workspace.setActiveLeaf(leaf, { focus: true });
    await app.workspace.openLinkText(destination, sourcePath, false);
  }
}
function getInternalLinkLeaf(app, settings, mode) {
  var _a, _b;
  if (mode === "window") {
    return app.workspace.getLeaf("window");
  }
  const direction = mode === "split-horizontal" ? "vertical" : "horizontal";
  const smartSplit = (_a = settings.smartSplit) != null ? _a : true;
  if (smartSplit) {
    const siblingTabs = findSiblingTabsContainer(app, direction);
    if (siblingTabs) {
      const children = (_b = siblingTabs.children) != null ? _b : [];
      return app.workspace.createLeafInParent(siblingTabs, children.length);
    }
  }
  return app.workspace.getLeaf("split", direction);
}
function findSiblingTabsContainer(app, direction) {
  var _a;
  const active = app.workspace.getLeaf(false);
  const tabsContainer = active.parent;
  if (!tabsContainer) return null;
  const splitContainer = tabsContainer.parent;
  if (!(splitContainer == null ? void 0 : splitContainer.direction) || splitContainer.direction !== direction) return null;
  const siblings = (_a = splitContainer.children) != null ? _a : [];
  if (siblings.length < 2) return null;
  for (const sibling of siblings) {
    if (sibling !== tabsContainer) {
      return sibling;
    }
  }
  return null;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/enums.js
var top = "top";
var bottom = "bottom";
var right = "right";
var left = "left";
var auto = "auto";
var basePlacements = [top, bottom, right, left];
var start = "start";
var end = "end";
var clippingParents = "clippingParents";
var viewport = "viewport";
var popper = "popper";
var reference = "reference";
var variationPlacements = /* @__PURE__ */ basePlacements.reduce(function(acc, placement) {
  return acc.concat([placement + "-" + start, placement + "-" + end]);
}, []);
var placements = /* @__PURE__ */ [].concat(basePlacements, [auto]).reduce(function(acc, placement) {
  return acc.concat([placement, placement + "-" + start, placement + "-" + end]);
}, []);
var beforeRead = "beforeRead";
var read = "read";
var afterRead = "afterRead";
var beforeMain = "beforeMain";
var main = "main";
var afterMain = "afterMain";
var beforeWrite = "beforeWrite";
var write = "write";
var afterWrite = "afterWrite";
var modifierPhases = [beforeRead, read, afterRead, beforeMain, main, afterMain, beforeWrite, write, afterWrite];

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getNodeName.js
function getNodeName(element) {
  return element ? (element.nodeName || "").toLowerCase() : null;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getWindow.js
function getWindow(node) {
  if (node == null) {
    return window;
  }
  if (node.toString() !== "[object Window]") {
    var ownerDocument = node.ownerDocument;
    return ownerDocument ? ownerDocument.defaultView || window : window;
  }
  return node;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/instanceOf.js
function isElement(node) {
  var OwnElement = getWindow(node).Element;
  return node instanceof OwnElement || node instanceof Element;
}
function isHTMLElement(node) {
  var OwnElement = getWindow(node).HTMLElement;
  return node instanceof OwnElement || node instanceof HTMLElement;
}
function isShadowRoot(node) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  var OwnElement = getWindow(node).ShadowRoot;
  return node instanceof OwnElement || node instanceof ShadowRoot;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/applyStyles.js
function applyStyles(_ref) {
  var state = _ref.state;
  Object.keys(state.elements).forEach(function(name) {
    var style = state.styles[name] || {};
    var attributes = state.attributes[name] || {};
    var element = state.elements[name];
    if (!isHTMLElement(element) || !getNodeName(element)) {
      return;
    }
    Object.assign(element.style, style);
    Object.keys(attributes).forEach(function(name2) {
      var value = attributes[name2];
      if (value === false) {
        element.removeAttribute(name2);
      } else {
        element.setAttribute(name2, value === true ? "" : value);
      }
    });
  });
}
function effect(_ref2) {
  var state = _ref2.state;
  var initialStyles = {
    popper: {
      position: state.options.strategy,
      left: "0",
      top: "0",
      margin: "0"
    },
    arrow: {
      position: "absolute"
    },
    reference: {}
  };
  Object.assign(state.elements.popper.style, initialStyles.popper);
  state.styles = initialStyles;
  if (state.elements.arrow) {
    Object.assign(state.elements.arrow.style, initialStyles.arrow);
  }
  return function() {
    Object.keys(state.elements).forEach(function(name) {
      var element = state.elements[name];
      var attributes = state.attributes[name] || {};
      var styleProperties = Object.keys(state.styles.hasOwnProperty(name) ? state.styles[name] : initialStyles[name]);
      var style = styleProperties.reduce(function(style2, property) {
        style2[property] = "";
        return style2;
      }, {});
      if (!isHTMLElement(element) || !getNodeName(element)) {
        return;
      }
      Object.assign(element.style, style);
      Object.keys(attributes).forEach(function(attribute) {
        element.removeAttribute(attribute);
      });
    });
  };
}
var applyStyles_default = {
  name: "applyStyles",
  enabled: true,
  phase: "write",
  fn: applyStyles,
  effect,
  requires: ["computeStyles"]
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getBasePlacement.js
function getBasePlacement(placement) {
  return placement.split("-")[0];
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/math.js
var max = Math.max;
var min = Math.min;
var round = Math.round;

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/userAgent.js
function getUAString() {
  var uaData = navigator.userAgentData;
  if (uaData != null && uaData.brands && Array.isArray(uaData.brands)) {
    return uaData.brands.map(function(item) {
      return item.brand + "/" + item.version;
    }).join(" ");
  }
  return navigator.userAgent;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/isLayoutViewport.js
function isLayoutViewport() {
  return !/^((?!chrome|android).)*safari/i.test(getUAString());
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getBoundingClientRect.js
function getBoundingClientRect(element, includeScale, isFixedStrategy) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  var clientRect = element.getBoundingClientRect();
  var scaleX = 1;
  var scaleY = 1;
  if (includeScale && isHTMLElement(element)) {
    scaleX = element.offsetWidth > 0 ? round(clientRect.width) / element.offsetWidth || 1 : 1;
    scaleY = element.offsetHeight > 0 ? round(clientRect.height) / element.offsetHeight || 1 : 1;
  }
  var _ref = isElement(element) ? getWindow(element) : window, visualViewport = _ref.visualViewport;
  var addVisualOffsets = !isLayoutViewport() && isFixedStrategy;
  var x = (clientRect.left + (addVisualOffsets && visualViewport ? visualViewport.offsetLeft : 0)) / scaleX;
  var y = (clientRect.top + (addVisualOffsets && visualViewport ? visualViewport.offsetTop : 0)) / scaleY;
  var width = clientRect.width / scaleX;
  var height = clientRect.height / scaleY;
  return {
    width,
    height,
    top: y,
    right: x + width,
    bottom: y + height,
    left: x,
    x,
    y
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getLayoutRect.js
function getLayoutRect(element) {
  var clientRect = getBoundingClientRect(element);
  var width = element.offsetWidth;
  var height = element.offsetHeight;
  if (Math.abs(clientRect.width - width) <= 1) {
    width = clientRect.width;
  }
  if (Math.abs(clientRect.height - height) <= 1) {
    height = clientRect.height;
  }
  return {
    x: element.offsetLeft,
    y: element.offsetTop,
    width,
    height
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/contains.js
function contains(parent, child) {
  var rootNode = child.getRootNode && child.getRootNode();
  if (parent.contains(child)) {
    return true;
  } else if (rootNode && isShadowRoot(rootNode)) {
    var next = child;
    do {
      if (next && parent.isSameNode(next)) {
        return true;
      }
      next = next.parentNode || next.host;
    } while (next);
  }
  return false;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getComputedStyle.js
function getComputedStyle(element) {
  return getWindow(element).getComputedStyle(element);
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/isTableElement.js
function isTableElement(element) {
  return ["table", "td", "th"].indexOf(getNodeName(element)) >= 0;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getDocumentElement.js
function getDocumentElement(element) {
  return ((isElement(element) ? element.ownerDocument : (
    // $FlowFixMe[prop-missing]
    element.document
  )) || window.document).documentElement;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getParentNode.js
function getParentNode(element) {
  if (getNodeName(element) === "html") {
    return element;
  }
  return (
    // this is a quicker (but less type safe) way to save quite some bytes from the bundle
    // $FlowFixMe[incompatible-return]
    // $FlowFixMe[prop-missing]
    element.assignedSlot || // step into the shadow DOM of the parent of a slotted node
    element.parentNode || // DOM Element detected
    (isShadowRoot(element) ? element.host : null) || // ShadowRoot detected
    // $FlowFixMe[incompatible-call]: HTMLElement is a Node
    getDocumentElement(element)
  );
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getOffsetParent.js
function getTrueOffsetParent(element) {
  if (!isHTMLElement(element) || // https://github.com/popperjs/popper-core/issues/837
  getComputedStyle(element).position === "fixed") {
    return null;
  }
  return element.offsetParent;
}
function getContainingBlock(element) {
  var isFirefox = /firefox/i.test(getUAString());
  var isIE = /Trident/i.test(getUAString());
  if (isIE && isHTMLElement(element)) {
    var elementCss = getComputedStyle(element);
    if (elementCss.position === "fixed") {
      return null;
    }
  }
  var currentNode = getParentNode(element);
  if (isShadowRoot(currentNode)) {
    currentNode = currentNode.host;
  }
  while (isHTMLElement(currentNode) && ["html", "body"].indexOf(getNodeName(currentNode)) < 0) {
    var css = getComputedStyle(currentNode);
    if (css.transform !== "none" || css.perspective !== "none" || css.contain === "paint" || ["transform", "perspective"].indexOf(css.willChange) !== -1 || isFirefox && css.willChange === "filter" || isFirefox && css.filter && css.filter !== "none") {
      return currentNode;
    } else {
      currentNode = currentNode.parentNode;
    }
  }
  return null;
}
function getOffsetParent(element) {
  var window2 = getWindow(element);
  var offsetParent = getTrueOffsetParent(element);
  while (offsetParent && isTableElement(offsetParent) && getComputedStyle(offsetParent).position === "static") {
    offsetParent = getTrueOffsetParent(offsetParent);
  }
  if (offsetParent && (getNodeName(offsetParent) === "html" || getNodeName(offsetParent) === "body" && getComputedStyle(offsetParent).position === "static")) {
    return window2;
  }
  return offsetParent || getContainingBlock(element) || window2;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getMainAxisFromPlacement.js
function getMainAxisFromPlacement(placement) {
  return ["top", "bottom"].indexOf(placement) >= 0 ? "x" : "y";
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/within.js
function within(min2, value, max2) {
  return max(min2, min(value, max2));
}
function withinMaxClamp(min2, value, max2) {
  var v = within(min2, value, max2);
  return v > max2 ? max2 : v;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getFreshSideObject.js
function getFreshSideObject() {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/mergePaddingObject.js
function mergePaddingObject(paddingObject) {
  return Object.assign({}, getFreshSideObject(), paddingObject);
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/expandToHashMap.js
function expandToHashMap(value, keys) {
  return keys.reduce(function(hashMap, key) {
    hashMap[key] = value;
    return hashMap;
  }, {});
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/arrow.js
var toPaddingObject = function toPaddingObject2(padding, state) {
  padding = typeof padding === "function" ? padding(Object.assign({}, state.rects, {
    placement: state.placement
  })) : padding;
  return mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
};
function arrow(_ref) {
  var _state$modifiersData$;
  var state = _ref.state, name = _ref.name, options = _ref.options;
  var arrowElement = state.elements.arrow;
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var basePlacement = getBasePlacement(state.placement);
  var axis = getMainAxisFromPlacement(basePlacement);
  var isVertical = [left, right].indexOf(basePlacement) >= 0;
  var len = isVertical ? "height" : "width";
  if (!arrowElement || !popperOffsets2) {
    return;
  }
  var paddingObject = toPaddingObject(options.padding, state);
  var arrowRect = getLayoutRect(arrowElement);
  var minProp = axis === "y" ? top : left;
  var maxProp = axis === "y" ? bottom : right;
  var endDiff = state.rects.reference[len] + state.rects.reference[axis] - popperOffsets2[axis] - state.rects.popper[len];
  var startDiff = popperOffsets2[axis] - state.rects.reference[axis];
  var arrowOffsetParent = getOffsetParent(arrowElement);
  var clientSize = arrowOffsetParent ? axis === "y" ? arrowOffsetParent.clientHeight || 0 : arrowOffsetParent.clientWidth || 0 : 0;
  var centerToReference = endDiff / 2 - startDiff / 2;
  var min2 = paddingObject[minProp];
  var max2 = clientSize - arrowRect[len] - paddingObject[maxProp];
  var center = clientSize / 2 - arrowRect[len] / 2 + centerToReference;
  var offset2 = within(min2, center, max2);
  var axisProp = axis;
  state.modifiersData[name] = (_state$modifiersData$ = {}, _state$modifiersData$[axisProp] = offset2, _state$modifiersData$.centerOffset = offset2 - center, _state$modifiersData$);
}
function effect2(_ref2) {
  var state = _ref2.state, options = _ref2.options;
  var _options$element = options.element, arrowElement = _options$element === void 0 ? "[data-popper-arrow]" : _options$element;
  if (arrowElement == null) {
    return;
  }
  if (typeof arrowElement === "string") {
    arrowElement = state.elements.popper.querySelector(arrowElement);
    if (!arrowElement) {
      return;
    }
  }
  if (!contains(state.elements.popper, arrowElement)) {
    return;
  }
  state.elements.arrow = arrowElement;
}
var arrow_default = {
  name: "arrow",
  enabled: true,
  phase: "main",
  fn: arrow,
  effect: effect2,
  requires: ["popperOffsets"],
  requiresIfExists: ["preventOverflow"]
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getVariation.js
function getVariation(placement) {
  return placement.split("-")[1];
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/computeStyles.js
var unsetSides = {
  top: "auto",
  right: "auto",
  bottom: "auto",
  left: "auto"
};
function roundOffsetsByDPR(_ref, win) {
  var x = _ref.x, y = _ref.y;
  var dpr = win.devicePixelRatio || 1;
  return {
    x: round(x * dpr) / dpr || 0,
    y: round(y * dpr) / dpr || 0
  };
}
function mapToStyles(_ref2) {
  var _Object$assign2;
  var popper2 = _ref2.popper, popperRect = _ref2.popperRect, placement = _ref2.placement, variation = _ref2.variation, offsets = _ref2.offsets, position = _ref2.position, gpuAcceleration = _ref2.gpuAcceleration, adaptive = _ref2.adaptive, roundOffsets = _ref2.roundOffsets, isFixed = _ref2.isFixed;
  var _offsets$x = offsets.x, x = _offsets$x === void 0 ? 0 : _offsets$x, _offsets$y = offsets.y, y = _offsets$y === void 0 ? 0 : _offsets$y;
  var _ref3 = typeof roundOffsets === "function" ? roundOffsets({
    x,
    y
  }) : {
    x,
    y
  };
  x = _ref3.x;
  y = _ref3.y;
  var hasX = offsets.hasOwnProperty("x");
  var hasY = offsets.hasOwnProperty("y");
  var sideX = left;
  var sideY = top;
  var win = window;
  if (adaptive) {
    var offsetParent = getOffsetParent(popper2);
    var heightProp = "clientHeight";
    var widthProp = "clientWidth";
    if (offsetParent === getWindow(popper2)) {
      offsetParent = getDocumentElement(popper2);
      if (getComputedStyle(offsetParent).position !== "static" && position === "absolute") {
        heightProp = "scrollHeight";
        widthProp = "scrollWidth";
      }
    }
    offsetParent = offsetParent;
    if (placement === top || (placement === left || placement === right) && variation === end) {
      sideY = bottom;
      var offsetY = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.height : (
        // $FlowFixMe[prop-missing]
        offsetParent[heightProp]
      );
      y -= offsetY - popperRect.height;
      y *= gpuAcceleration ? 1 : -1;
    }
    if (placement === left || (placement === top || placement === bottom) && variation === end) {
      sideX = right;
      var offsetX = isFixed && offsetParent === win && win.visualViewport ? win.visualViewport.width : (
        // $FlowFixMe[prop-missing]
        offsetParent[widthProp]
      );
      x -= offsetX - popperRect.width;
      x *= gpuAcceleration ? 1 : -1;
    }
  }
  var commonStyles = Object.assign({
    position
  }, adaptive && unsetSides);
  var _ref4 = roundOffsets === true ? roundOffsetsByDPR({
    x,
    y
  }, getWindow(popper2)) : {
    x,
    y
  };
  x = _ref4.x;
  y = _ref4.y;
  if (gpuAcceleration) {
    var _Object$assign;
    return Object.assign({}, commonStyles, (_Object$assign = {}, _Object$assign[sideY] = hasY ? "0" : "", _Object$assign[sideX] = hasX ? "0" : "", _Object$assign.transform = (win.devicePixelRatio || 1) <= 1 ? "translate(" + x + "px, " + y + "px)" : "translate3d(" + x + "px, " + y + "px, 0)", _Object$assign));
  }
  return Object.assign({}, commonStyles, (_Object$assign2 = {}, _Object$assign2[sideY] = hasY ? y + "px" : "", _Object$assign2[sideX] = hasX ? x + "px" : "", _Object$assign2.transform = "", _Object$assign2));
}
function computeStyles(_ref5) {
  var state = _ref5.state, options = _ref5.options;
  var _options$gpuAccelerat = options.gpuAcceleration, gpuAcceleration = _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat, _options$adaptive = options.adaptive, adaptive = _options$adaptive === void 0 ? true : _options$adaptive, _options$roundOffsets = options.roundOffsets, roundOffsets = _options$roundOffsets === void 0 ? true : _options$roundOffsets;
  var commonStyles = {
    placement: getBasePlacement(state.placement),
    variation: getVariation(state.placement),
    popper: state.elements.popper,
    popperRect: state.rects.popper,
    gpuAcceleration,
    isFixed: state.options.strategy === "fixed"
  };
  if (state.modifiersData.popperOffsets != null) {
    state.styles.popper = Object.assign({}, state.styles.popper, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.popperOffsets,
      position: state.options.strategy,
      adaptive,
      roundOffsets
    })));
  }
  if (state.modifiersData.arrow != null) {
    state.styles.arrow = Object.assign({}, state.styles.arrow, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.arrow,
      position: "absolute",
      adaptive: false,
      roundOffsets
    })));
  }
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-placement": state.placement
  });
}
var computeStyles_default = {
  name: "computeStyles",
  enabled: true,
  phase: "beforeWrite",
  fn: computeStyles,
  data: {}
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/eventListeners.js
var passive = {
  passive: true
};
function effect3(_ref) {
  var state = _ref.state, instance = _ref.instance, options = _ref.options;
  var _options$scroll = options.scroll, scroll = _options$scroll === void 0 ? true : _options$scroll, _options$resize = options.resize, resize = _options$resize === void 0 ? true : _options$resize;
  var window2 = getWindow(state.elements.popper);
  var scrollParents = [].concat(state.scrollParents.reference, state.scrollParents.popper);
  if (scroll) {
    scrollParents.forEach(function(scrollParent) {
      scrollParent.addEventListener("scroll", instance.update, passive);
    });
  }
  if (resize) {
    window2.addEventListener("resize", instance.update, passive);
  }
  return function() {
    if (scroll) {
      scrollParents.forEach(function(scrollParent) {
        scrollParent.removeEventListener("scroll", instance.update, passive);
      });
    }
    if (resize) {
      window2.removeEventListener("resize", instance.update, passive);
    }
  };
}
var eventListeners_default = {
  name: "eventListeners",
  enabled: true,
  phase: "write",
  fn: function fn() {
  },
  effect: effect3,
  data: {}
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getOppositePlacement.js
var hash = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, function(matched) {
    return hash[matched];
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getOppositeVariationPlacement.js
var hash2 = {
  start: "end",
  end: "start"
};
function getOppositeVariationPlacement(placement) {
  return placement.replace(/start|end/g, function(matched) {
    return hash2[matched];
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getWindowScroll.js
function getWindowScroll(node) {
  var win = getWindow(node);
  var scrollLeft = win.pageXOffset;
  var scrollTop = win.pageYOffset;
  return {
    scrollLeft,
    scrollTop
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getWindowScrollBarX.js
function getWindowScrollBarX(element) {
  return getBoundingClientRect(getDocumentElement(element)).left + getWindowScroll(element).scrollLeft;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getViewportRect.js
function getViewportRect(element, strategy) {
  var win = getWindow(element);
  var html = getDocumentElement(element);
  var visualViewport = win.visualViewport;
  var width = html.clientWidth;
  var height = html.clientHeight;
  var x = 0;
  var y = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height = visualViewport.height;
    var layoutViewport = isLayoutViewport();
    if (layoutViewport || !layoutViewport && strategy === "fixed") {
      x = visualViewport.offsetLeft;
      y = visualViewport.offsetTop;
    }
  }
  return {
    width,
    height,
    x: x + getWindowScrollBarX(element),
    y
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getDocumentRect.js
function getDocumentRect(element) {
  var _element$ownerDocumen;
  var html = getDocumentElement(element);
  var winScroll = getWindowScroll(element);
  var body = (_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body;
  var width = max(html.scrollWidth, html.clientWidth, body ? body.scrollWidth : 0, body ? body.clientWidth : 0);
  var height = max(html.scrollHeight, html.clientHeight, body ? body.scrollHeight : 0, body ? body.clientHeight : 0);
  var x = -winScroll.scrollLeft + getWindowScrollBarX(element);
  var y = -winScroll.scrollTop;
  if (getComputedStyle(body || html).direction === "rtl") {
    x += max(html.clientWidth, body ? body.clientWidth : 0) - width;
  }
  return {
    width,
    height,
    x,
    y
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/isScrollParent.js
function isScrollParent(element) {
  var _getComputedStyle = getComputedStyle(element), overflow = _getComputedStyle.overflow, overflowX = _getComputedStyle.overflowX, overflowY = _getComputedStyle.overflowY;
  return /auto|scroll|overlay|hidden/.test(overflow + overflowY + overflowX);
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getScrollParent.js
function getScrollParent(node) {
  if (["html", "body", "#document"].indexOf(getNodeName(node)) >= 0) {
    return node.ownerDocument.body;
  }
  if (isHTMLElement(node) && isScrollParent(node)) {
    return node;
  }
  return getScrollParent(getParentNode(node));
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/listScrollParents.js
function listScrollParents(element, list) {
  var _element$ownerDocumen;
  if (list === void 0) {
    list = [];
  }
  var scrollParent = getScrollParent(element);
  var isBody = scrollParent === ((_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body);
  var win = getWindow(scrollParent);
  var target = isBody ? [win].concat(win.visualViewport || [], isScrollParent(scrollParent) ? scrollParent : []) : scrollParent;
  var updatedList = list.concat(target);
  return isBody ? updatedList : (
    // $FlowFixMe[incompatible-call]: isBody tells us target will be an HTMLElement here
    updatedList.concat(listScrollParents(getParentNode(target)))
  );
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/rectToClientRect.js
function rectToClientRect(rect) {
  return Object.assign({}, rect, {
    left: rect.x,
    top: rect.y,
    right: rect.x + rect.width,
    bottom: rect.y + rect.height
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getClippingRect.js
function getInnerBoundingClientRect(element, strategy) {
  var rect = getBoundingClientRect(element, false, strategy === "fixed");
  rect.top = rect.top + element.clientTop;
  rect.left = rect.left + element.clientLeft;
  rect.bottom = rect.top + element.clientHeight;
  rect.right = rect.left + element.clientWidth;
  rect.width = element.clientWidth;
  rect.height = element.clientHeight;
  rect.x = rect.left;
  rect.y = rect.top;
  return rect;
}
function getClientRectFromMixedType(element, clippingParent, strategy) {
  return clippingParent === viewport ? rectToClientRect(getViewportRect(element, strategy)) : isElement(clippingParent) ? getInnerBoundingClientRect(clippingParent, strategy) : rectToClientRect(getDocumentRect(getDocumentElement(element)));
}
function getClippingParents(element) {
  var clippingParents2 = listScrollParents(getParentNode(element));
  var canEscapeClipping = ["absolute", "fixed"].indexOf(getComputedStyle(element).position) >= 0;
  var clipperElement = canEscapeClipping && isHTMLElement(element) ? getOffsetParent(element) : element;
  if (!isElement(clipperElement)) {
    return [];
  }
  return clippingParents2.filter(function(clippingParent) {
    return isElement(clippingParent) && contains(clippingParent, clipperElement) && getNodeName(clippingParent) !== "body";
  });
}
function getClippingRect(element, boundary, rootBoundary, strategy) {
  var mainClippingParents = boundary === "clippingParents" ? getClippingParents(element) : [].concat(boundary);
  var clippingParents2 = [].concat(mainClippingParents, [rootBoundary]);
  var firstClippingParent = clippingParents2[0];
  var clippingRect = clippingParents2.reduce(function(accRect, clippingParent) {
    var rect = getClientRectFromMixedType(element, clippingParent, strategy);
    accRect.top = max(rect.top, accRect.top);
    accRect.right = min(rect.right, accRect.right);
    accRect.bottom = min(rect.bottom, accRect.bottom);
    accRect.left = max(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromMixedType(element, firstClippingParent, strategy));
  clippingRect.width = clippingRect.right - clippingRect.left;
  clippingRect.height = clippingRect.bottom - clippingRect.top;
  clippingRect.x = clippingRect.left;
  clippingRect.y = clippingRect.top;
  return clippingRect;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/computeOffsets.js
function computeOffsets(_ref) {
  var reference2 = _ref.reference, element = _ref.element, placement = _ref.placement;
  var basePlacement = placement ? getBasePlacement(placement) : null;
  var variation = placement ? getVariation(placement) : null;
  var commonX = reference2.x + reference2.width / 2 - element.width / 2;
  var commonY = reference2.y + reference2.height / 2 - element.height / 2;
  var offsets;
  switch (basePlacement) {
    case top:
      offsets = {
        x: commonX,
        y: reference2.y - element.height
      };
      break;
    case bottom:
      offsets = {
        x: commonX,
        y: reference2.y + reference2.height
      };
      break;
    case right:
      offsets = {
        x: reference2.x + reference2.width,
        y: commonY
      };
      break;
    case left:
      offsets = {
        x: reference2.x - element.width,
        y: commonY
      };
      break;
    default:
      offsets = {
        x: reference2.x,
        y: reference2.y
      };
  }
  var mainAxis = basePlacement ? getMainAxisFromPlacement(basePlacement) : null;
  if (mainAxis != null) {
    var len = mainAxis === "y" ? "height" : "width";
    switch (variation) {
      case start:
        offsets[mainAxis] = offsets[mainAxis] - (reference2[len] / 2 - element[len] / 2);
        break;
      case end:
        offsets[mainAxis] = offsets[mainAxis] + (reference2[len] / 2 - element[len] / 2);
        break;
      default:
    }
  }
  return offsets;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/detectOverflow.js
function detectOverflow(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, _options$placement = _options.placement, placement = _options$placement === void 0 ? state.placement : _options$placement, _options$strategy = _options.strategy, strategy = _options$strategy === void 0 ? state.strategy : _options$strategy, _options$boundary = _options.boundary, boundary = _options$boundary === void 0 ? clippingParents : _options$boundary, _options$rootBoundary = _options.rootBoundary, rootBoundary = _options$rootBoundary === void 0 ? viewport : _options$rootBoundary, _options$elementConte = _options.elementContext, elementContext = _options$elementConte === void 0 ? popper : _options$elementConte, _options$altBoundary = _options.altBoundary, altBoundary = _options$altBoundary === void 0 ? false : _options$altBoundary, _options$padding = _options.padding, padding = _options$padding === void 0 ? 0 : _options$padding;
  var paddingObject = mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
  var altContext = elementContext === popper ? reference : popper;
  var popperRect = state.rects.popper;
  var element = state.elements[altBoundary ? altContext : elementContext];
  var clippingClientRect = getClippingRect(isElement(element) ? element : element.contextElement || getDocumentElement(state.elements.popper), boundary, rootBoundary, strategy);
  var referenceClientRect = getBoundingClientRect(state.elements.reference);
  var popperOffsets2 = computeOffsets({
    reference: referenceClientRect,
    element: popperRect,
    strategy: "absolute",
    placement
  });
  var popperClientRect = rectToClientRect(Object.assign({}, popperRect, popperOffsets2));
  var elementClientRect = elementContext === popper ? popperClientRect : referenceClientRect;
  var overflowOffsets = {
    top: clippingClientRect.top - elementClientRect.top + paddingObject.top,
    bottom: elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom,
    left: clippingClientRect.left - elementClientRect.left + paddingObject.left,
    right: elementClientRect.right - clippingClientRect.right + paddingObject.right
  };
  var offsetData = state.modifiersData.offset;
  if (elementContext === popper && offsetData) {
    var offset2 = offsetData[placement];
    Object.keys(overflowOffsets).forEach(function(key) {
      var multiply = [right, bottom].indexOf(key) >= 0 ? 1 : -1;
      var axis = [top, bottom].indexOf(key) >= 0 ? "y" : "x";
      overflowOffsets[key] += offset2[axis] * multiply;
    });
  }
  return overflowOffsets;
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/computeAutoPlacement.js
function computeAutoPlacement(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, placement = _options.placement, boundary = _options.boundary, rootBoundary = _options.rootBoundary, padding = _options.padding, flipVariations = _options.flipVariations, _options$allowedAutoP = _options.allowedAutoPlacements, allowedAutoPlacements = _options$allowedAutoP === void 0 ? placements : _options$allowedAutoP;
  var variation = getVariation(placement);
  var placements2 = variation ? flipVariations ? variationPlacements : variationPlacements.filter(function(placement2) {
    return getVariation(placement2) === variation;
  }) : basePlacements;
  var allowedPlacements = placements2.filter(function(placement2) {
    return allowedAutoPlacements.indexOf(placement2) >= 0;
  });
  if (allowedPlacements.length === 0) {
    allowedPlacements = placements2;
  }
  var overflows = allowedPlacements.reduce(function(acc, placement2) {
    acc[placement2] = detectOverflow(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding
    })[getBasePlacement(placement2)];
    return acc;
  }, {});
  return Object.keys(overflows).sort(function(a, b) {
    return overflows[a] - overflows[b];
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/flip.js
function getExpandedFallbackPlacements(placement) {
  if (getBasePlacement(placement) === auto) {
    return [];
  }
  var oppositePlacement = getOppositePlacement(placement);
  return [getOppositeVariationPlacement(placement), oppositePlacement, getOppositeVariationPlacement(oppositePlacement)];
}
function flip(_ref) {
  var state = _ref.state, options = _ref.options, name = _ref.name;
  if (state.modifiersData[name]._skip) {
    return;
  }
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? true : _options$altAxis, specifiedFallbackPlacements = options.fallbackPlacements, padding = options.padding, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, _options$flipVariatio = options.flipVariations, flipVariations = _options$flipVariatio === void 0 ? true : _options$flipVariatio, allowedAutoPlacements = options.allowedAutoPlacements;
  var preferredPlacement = state.options.placement;
  var basePlacement = getBasePlacement(preferredPlacement);
  var isBasePlacement = basePlacement === preferredPlacement;
  var fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipVariations ? [getOppositePlacement(preferredPlacement)] : getExpandedFallbackPlacements(preferredPlacement));
  var placements2 = [preferredPlacement].concat(fallbackPlacements).reduce(function(acc, placement2) {
    return acc.concat(getBasePlacement(placement2) === auto ? computeAutoPlacement(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding,
      flipVariations,
      allowedAutoPlacements
    }) : placement2);
  }, []);
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var checksMap = /* @__PURE__ */ new Map();
  var makeFallbackChecks = true;
  var firstFittingPlacement = placements2[0];
  for (var i = 0; i < placements2.length; i++) {
    var placement = placements2[i];
    var _basePlacement = getBasePlacement(placement);
    var isStartVariation = getVariation(placement) === start;
    var isVertical = [top, bottom].indexOf(_basePlacement) >= 0;
    var len = isVertical ? "width" : "height";
    var overflow = detectOverflow(state, {
      placement,
      boundary,
      rootBoundary,
      altBoundary,
      padding
    });
    var mainVariationSide = isVertical ? isStartVariation ? right : left : isStartVariation ? bottom : top;
    if (referenceRect[len] > popperRect[len]) {
      mainVariationSide = getOppositePlacement(mainVariationSide);
    }
    var altVariationSide = getOppositePlacement(mainVariationSide);
    var checks = [];
    if (checkMainAxis) {
      checks.push(overflow[_basePlacement] <= 0);
    }
    if (checkAltAxis) {
      checks.push(overflow[mainVariationSide] <= 0, overflow[altVariationSide] <= 0);
    }
    if (checks.every(function(check) {
      return check;
    })) {
      firstFittingPlacement = placement;
      makeFallbackChecks = false;
      break;
    }
    checksMap.set(placement, checks);
  }
  if (makeFallbackChecks) {
    var numberOfChecks = flipVariations ? 3 : 1;
    var _loop = function _loop2(_i2) {
      var fittingPlacement = placements2.find(function(placement2) {
        var checks2 = checksMap.get(placement2);
        if (checks2) {
          return checks2.slice(0, _i2).every(function(check) {
            return check;
          });
        }
      });
      if (fittingPlacement) {
        firstFittingPlacement = fittingPlacement;
        return "break";
      }
    };
    for (var _i = numberOfChecks; _i > 0; _i--) {
      var _ret = _loop(_i);
      if (_ret === "break") break;
    }
  }
  if (state.placement !== firstFittingPlacement) {
    state.modifiersData[name]._skip = true;
    state.placement = firstFittingPlacement;
    state.reset = true;
  }
}
var flip_default = {
  name: "flip",
  enabled: true,
  phase: "main",
  fn: flip,
  requiresIfExists: ["offset"],
  data: {
    _skip: false
  }
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/hide.js
function getSideOffsets(overflow, rect, preventedOffsets) {
  if (preventedOffsets === void 0) {
    preventedOffsets = {
      x: 0,
      y: 0
    };
  }
  return {
    top: overflow.top - rect.height - preventedOffsets.y,
    right: overflow.right - rect.width + preventedOffsets.x,
    bottom: overflow.bottom - rect.height + preventedOffsets.y,
    left: overflow.left - rect.width - preventedOffsets.x
  };
}
function isAnySideFullyClipped(overflow) {
  return [top, right, bottom, left].some(function(side) {
    return overflow[side] >= 0;
  });
}
function hide(_ref) {
  var state = _ref.state, name = _ref.name;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var preventedOffsets = state.modifiersData.preventOverflow;
  var referenceOverflow = detectOverflow(state, {
    elementContext: "reference"
  });
  var popperAltOverflow = detectOverflow(state, {
    altBoundary: true
  });
  var referenceClippingOffsets = getSideOffsets(referenceOverflow, referenceRect);
  var popperEscapeOffsets = getSideOffsets(popperAltOverflow, popperRect, preventedOffsets);
  var isReferenceHidden = isAnySideFullyClipped(referenceClippingOffsets);
  var hasPopperEscaped = isAnySideFullyClipped(popperEscapeOffsets);
  state.modifiersData[name] = {
    referenceClippingOffsets,
    popperEscapeOffsets,
    isReferenceHidden,
    hasPopperEscaped
  };
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-reference-hidden": isReferenceHidden,
    "data-popper-escaped": hasPopperEscaped
  });
}
var hide_default = {
  name: "hide",
  enabled: true,
  phase: "main",
  requiresIfExists: ["preventOverflow"],
  fn: hide
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/offset.js
function distanceAndSkiddingToXY(placement, rects, offset2) {
  var basePlacement = getBasePlacement(placement);
  var invertDistance = [left, top].indexOf(basePlacement) >= 0 ? -1 : 1;
  var _ref = typeof offset2 === "function" ? offset2(Object.assign({}, rects, {
    placement
  })) : offset2, skidding = _ref[0], distance = _ref[1];
  skidding = skidding || 0;
  distance = (distance || 0) * invertDistance;
  return [left, right].indexOf(basePlacement) >= 0 ? {
    x: distance,
    y: skidding
  } : {
    x: skidding,
    y: distance
  };
}
function offset(_ref2) {
  var state = _ref2.state, options = _ref2.options, name = _ref2.name;
  var _options$offset = options.offset, offset2 = _options$offset === void 0 ? [0, 0] : _options$offset;
  var data = placements.reduce(function(acc, placement) {
    acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset2);
    return acc;
  }, {});
  var _data$state$placement = data[state.placement], x = _data$state$placement.x, y = _data$state$placement.y;
  if (state.modifiersData.popperOffsets != null) {
    state.modifiersData.popperOffsets.x += x;
    state.modifiersData.popperOffsets.y += y;
  }
  state.modifiersData[name] = data;
}
var offset_default = {
  name: "offset",
  enabled: true,
  phase: "main",
  requires: ["popperOffsets"],
  fn: offset
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/popperOffsets.js
function popperOffsets(_ref) {
  var state = _ref.state, name = _ref.name;
  state.modifiersData[name] = computeOffsets({
    reference: state.rects.reference,
    element: state.rects.popper,
    strategy: "absolute",
    placement: state.placement
  });
}
var popperOffsets_default = {
  name: "popperOffsets",
  enabled: true,
  phase: "read",
  fn: popperOffsets,
  data: {}
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/getAltAxis.js
function getAltAxis(axis) {
  return axis === "x" ? "y" : "x";
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/modifiers/preventOverflow.js
function preventOverflow(_ref) {
  var state = _ref.state, options = _ref.options, name = _ref.name;
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? false : _options$altAxis, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, padding = options.padding, _options$tether = options.tether, tether = _options$tether === void 0 ? true : _options$tether, _options$tetherOffset = options.tetherOffset, tetherOffset = _options$tetherOffset === void 0 ? 0 : _options$tetherOffset;
  var overflow = detectOverflow(state, {
    boundary,
    rootBoundary,
    padding,
    altBoundary
  });
  var basePlacement = getBasePlacement(state.placement);
  var variation = getVariation(state.placement);
  var isBasePlacement = !variation;
  var mainAxis = getMainAxisFromPlacement(basePlacement);
  var altAxis = getAltAxis(mainAxis);
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var tetherOffsetValue = typeof tetherOffset === "function" ? tetherOffset(Object.assign({}, state.rects, {
    placement: state.placement
  })) : tetherOffset;
  var normalizedTetherOffsetValue = typeof tetherOffsetValue === "number" ? {
    mainAxis: tetherOffsetValue,
    altAxis: tetherOffsetValue
  } : Object.assign({
    mainAxis: 0,
    altAxis: 0
  }, tetherOffsetValue);
  var offsetModifierState = state.modifiersData.offset ? state.modifiersData.offset[state.placement] : null;
  var data = {
    x: 0,
    y: 0
  };
  if (!popperOffsets2) {
    return;
  }
  if (checkMainAxis) {
    var _offsetModifierState$;
    var mainSide = mainAxis === "y" ? top : left;
    var altSide = mainAxis === "y" ? bottom : right;
    var len = mainAxis === "y" ? "height" : "width";
    var offset2 = popperOffsets2[mainAxis];
    var min2 = offset2 + overflow[mainSide];
    var max2 = offset2 - overflow[altSide];
    var additive = tether ? -popperRect[len] / 2 : 0;
    var minLen = variation === start ? referenceRect[len] : popperRect[len];
    var maxLen = variation === start ? -popperRect[len] : -referenceRect[len];
    var arrowElement = state.elements.arrow;
    var arrowRect = tether && arrowElement ? getLayoutRect(arrowElement) : {
      width: 0,
      height: 0
    };
    var arrowPaddingObject = state.modifiersData["arrow#persistent"] ? state.modifiersData["arrow#persistent"].padding : getFreshSideObject();
    var arrowPaddingMin = arrowPaddingObject[mainSide];
    var arrowPaddingMax = arrowPaddingObject[altSide];
    var arrowLen = within(0, referenceRect[len], arrowRect[len]);
    var minOffset = isBasePlacement ? referenceRect[len] / 2 - additive - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis : minLen - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis;
    var maxOffset = isBasePlacement ? -referenceRect[len] / 2 + additive + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis : maxLen + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis;
    var arrowOffsetParent = state.elements.arrow && getOffsetParent(state.elements.arrow);
    var clientOffset = arrowOffsetParent ? mainAxis === "y" ? arrowOffsetParent.clientTop || 0 : arrowOffsetParent.clientLeft || 0 : 0;
    var offsetModifierValue = (_offsetModifierState$ = offsetModifierState == null ? void 0 : offsetModifierState[mainAxis]) != null ? _offsetModifierState$ : 0;
    var tetherMin = offset2 + minOffset - offsetModifierValue - clientOffset;
    var tetherMax = offset2 + maxOffset - offsetModifierValue;
    var preventedOffset = within(tether ? min(min2, tetherMin) : min2, offset2, tether ? max(max2, tetherMax) : max2);
    popperOffsets2[mainAxis] = preventedOffset;
    data[mainAxis] = preventedOffset - offset2;
  }
  if (checkAltAxis) {
    var _offsetModifierState$2;
    var _mainSide = mainAxis === "x" ? top : left;
    var _altSide = mainAxis === "x" ? bottom : right;
    var _offset = popperOffsets2[altAxis];
    var _len = altAxis === "y" ? "height" : "width";
    var _min = _offset + overflow[_mainSide];
    var _max = _offset - overflow[_altSide];
    var isOriginSide = [top, left].indexOf(basePlacement) !== -1;
    var _offsetModifierValue = (_offsetModifierState$2 = offsetModifierState == null ? void 0 : offsetModifierState[altAxis]) != null ? _offsetModifierState$2 : 0;
    var _tetherMin = isOriginSide ? _min : _offset - referenceRect[_len] - popperRect[_len] - _offsetModifierValue + normalizedTetherOffsetValue.altAxis;
    var _tetherMax = isOriginSide ? _offset + referenceRect[_len] + popperRect[_len] - _offsetModifierValue - normalizedTetherOffsetValue.altAxis : _max;
    var _preventedOffset = tether && isOriginSide ? withinMaxClamp(_tetherMin, _offset, _tetherMax) : within(tether ? _tetherMin : _min, _offset, tether ? _tetherMax : _max);
    popperOffsets2[altAxis] = _preventedOffset;
    data[altAxis] = _preventedOffset - _offset;
  }
  state.modifiersData[name] = data;
}
var preventOverflow_default = {
  name: "preventOverflow",
  enabled: true,
  phase: "main",
  fn: preventOverflow,
  requiresIfExists: ["offset"]
};

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getHTMLElementScroll.js
function getHTMLElementScroll(element) {
  return {
    scrollLeft: element.scrollLeft,
    scrollTop: element.scrollTop
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getNodeScroll.js
function getNodeScroll(node) {
  if (node === getWindow(node) || !isHTMLElement(node)) {
    return getWindowScroll(node);
  } else {
    return getHTMLElementScroll(node);
  }
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/dom-utils/getCompositeRect.js
function isElementScaled(element) {
  var rect = element.getBoundingClientRect();
  var scaleX = round(rect.width) / element.offsetWidth || 1;
  var scaleY = round(rect.height) / element.offsetHeight || 1;
  return scaleX !== 1 || scaleY !== 1;
}
function getCompositeRect(elementOrVirtualElement, offsetParent, isFixed) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  var isOffsetParentAnElement = isHTMLElement(offsetParent);
  var offsetParentIsScaled = isHTMLElement(offsetParent) && isElementScaled(offsetParent);
  var documentElement = getDocumentElement(offsetParent);
  var rect = getBoundingClientRect(elementOrVirtualElement, offsetParentIsScaled, isFixed);
  var scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  var offsets = {
    x: 0,
    y: 0
  };
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || // https://github.com/popperjs/popper-core/issues/1078
    isScrollParent(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isHTMLElement(offsetParent)) {
      offsets = getBoundingClientRect(offsetParent, true);
      offsets.x += offsetParent.clientLeft;
      offsets.y += offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
  }
  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/orderModifiers.js
function order(modifiers) {
  var map = /* @__PURE__ */ new Map();
  var visited = /* @__PURE__ */ new Set();
  var result = [];
  modifiers.forEach(function(modifier) {
    map.set(modifier.name, modifier);
  });
  function sort(modifier) {
    visited.add(modifier.name);
    var requires = [].concat(modifier.requires || [], modifier.requiresIfExists || []);
    requires.forEach(function(dep) {
      if (!visited.has(dep)) {
        var depModifier = map.get(dep);
        if (depModifier) {
          sort(depModifier);
        }
      }
    });
    result.push(modifier);
  }
  modifiers.forEach(function(modifier) {
    if (!visited.has(modifier.name)) {
      sort(modifier);
    }
  });
  return result;
}
function orderModifiers(modifiers) {
  var orderedModifiers = order(modifiers);
  return modifierPhases.reduce(function(acc, phase) {
    return acc.concat(orderedModifiers.filter(function(modifier) {
      return modifier.phase === phase;
    }));
  }, []);
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/debounce.js
function debounce(fn2) {
  var pending;
  return function() {
    if (!pending) {
      pending = new Promise(function(resolve) {
        Promise.resolve().then(function() {
          pending = void 0;
          resolve(fn2());
        });
      });
    }
    return pending;
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/utils/mergeByName.js
function mergeByName(modifiers) {
  var merged = modifiers.reduce(function(merged2, current) {
    var existing = merged2[current.name];
    merged2[current.name] = existing ? Object.assign({}, existing, current, {
      options: Object.assign({}, existing.options, current.options),
      data: Object.assign({}, existing.data, current.data)
    }) : current;
    return merged2;
  }, {});
  return Object.keys(merged).map(function(key) {
    return merged[key];
  });
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/createPopper.js
var DEFAULT_OPTIONS = {
  placement: "bottom",
  modifiers: [],
  strategy: "absolute"
};
function areValidElements() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return !args.some(function(element) {
    return !(element && typeof element.getBoundingClientRect === "function");
  });
}
function popperGenerator(generatorOptions) {
  if (generatorOptions === void 0) {
    generatorOptions = {};
  }
  var _generatorOptions = generatorOptions, _generatorOptions$def = _generatorOptions.defaultModifiers, defaultModifiers2 = _generatorOptions$def === void 0 ? [] : _generatorOptions$def, _generatorOptions$def2 = _generatorOptions.defaultOptions, defaultOptions = _generatorOptions$def2 === void 0 ? DEFAULT_OPTIONS : _generatorOptions$def2;
  return function createPopper2(reference2, popper2, options) {
    if (options === void 0) {
      options = defaultOptions;
    }
    var state = {
      placement: "bottom",
      orderedModifiers: [],
      options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions),
      modifiersData: {},
      elements: {
        reference: reference2,
        popper: popper2
      },
      attributes: {},
      styles: {}
    };
    var effectCleanupFns = [];
    var isDestroyed = false;
    var instance = {
      state,
      setOptions: function setOptions(setOptionsAction) {
        var options2 = typeof setOptionsAction === "function" ? setOptionsAction(state.options) : setOptionsAction;
        cleanupModifierEffects();
        state.options = Object.assign({}, defaultOptions, state.options, options2);
        state.scrollParents = {
          reference: isElement(reference2) ? listScrollParents(reference2) : reference2.contextElement ? listScrollParents(reference2.contextElement) : [],
          popper: listScrollParents(popper2)
        };
        var orderedModifiers = orderModifiers(mergeByName([].concat(defaultModifiers2, state.options.modifiers)));
        state.orderedModifiers = orderedModifiers.filter(function(m) {
          return m.enabled;
        });
        runModifierEffects();
        return instance.update();
      },
      // Sync update – it will always be executed, even if not necessary. This
      // is useful for low frequency updates where sync behavior simplifies the
      // logic.
      // For high frequency updates (e.g. `resize` and `scroll` events), always
      // prefer the async Popper#update method
      forceUpdate: function forceUpdate() {
        if (isDestroyed) {
          return;
        }
        var _state$elements = state.elements, reference3 = _state$elements.reference, popper3 = _state$elements.popper;
        if (!areValidElements(reference3, popper3)) {
          return;
        }
        state.rects = {
          reference: getCompositeRect(reference3, getOffsetParent(popper3), state.options.strategy === "fixed"),
          popper: getLayoutRect(popper3)
        };
        state.reset = false;
        state.placement = state.options.placement;
        state.orderedModifiers.forEach(function(modifier) {
          return state.modifiersData[modifier.name] = Object.assign({}, modifier.data);
        });
        for (var index = 0; index < state.orderedModifiers.length; index++) {
          if (state.reset === true) {
            state.reset = false;
            index = -1;
            continue;
          }
          var _state$orderedModifie = state.orderedModifiers[index], fn2 = _state$orderedModifie.fn, _state$orderedModifie2 = _state$orderedModifie.options, _options = _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2, name = _state$orderedModifie.name;
          if (typeof fn2 === "function") {
            state = fn2({
              state,
              options: _options,
              name,
              instance
            }) || state;
          }
        }
      },
      // Async and optimistically optimized update – it will not be executed if
      // not necessary (debounced to run at most once-per-tick)
      update: debounce(function() {
        return new Promise(function(resolve) {
          instance.forceUpdate();
          resolve(state);
        });
      }),
      destroy: function destroy() {
        cleanupModifierEffects();
        isDestroyed = true;
      }
    };
    if (!areValidElements(reference2, popper2)) {
      return instance;
    }
    instance.setOptions(options).then(function(state2) {
      if (!isDestroyed && options.onFirstUpdate) {
        options.onFirstUpdate(state2);
      }
    });
    function runModifierEffects() {
      state.orderedModifiers.forEach(function(_ref) {
        var name = _ref.name, _ref$options = _ref.options, options2 = _ref$options === void 0 ? {} : _ref$options, effect4 = _ref.effect;
        if (typeof effect4 === "function") {
          var cleanupFn = effect4({
            state,
            name,
            instance,
            options: options2
          });
          var noopFn = function noopFn2() {
          };
          effectCleanupFns.push(cleanupFn || noopFn);
        }
      });
    }
    function cleanupModifierEffects() {
      effectCleanupFns.forEach(function(fn2) {
        return fn2();
      });
      effectCleanupFns = [];
    }
    return instance;
  };
}

// node_modules/.pnpm/@popperjs+core@2.11.8/node_modules/@popperjs/core/lib/popper.js
var defaultModifiers = [eventListeners_default, popperOffsets_default, computeStyles_default, applyStyles_default, offset_default, flip_default, preventOverflow_default, arrow_default, hide_default];
var createPopper = /* @__PURE__ */ popperGenerator({
  defaultModifiers
});

// src/popoverEditor.ts
var import_obsidian2 = require("obsidian");
var PopoverEditor = class {
  constructor(events, t2) {
    this.events = events;
    this.t = t2;
    this.popperInstance = null;
    this.outsidePointerDownHandler = null;
    this.keydownHandler = null;
    this.scrollHandler = null;
    this.resizeHandler = null;
    this.updateRafId = null;
    this.isSuggestActiveChecker = null;
    /** 当前 popover 挂载到的 document（可能是主窗口或 popout window） */
    this.currentDoc = activeDocument;
    this.wrapperEl = activeDocument.body.createDiv({ cls: "better-links-popover-wrapper" });
    this.wrapperEl.hide();
    this.rootEl = this.wrapperEl.createDiv({ cls: "better-links-popover" });
    const badgeRow = this.rootEl.createDiv({ cls: "better-links-popover__badge-row" });
    this.typeBadgeEl = badgeRow.createSpan({ cls: "better-links-popover__badge" });
    const formEl = this.rootEl.createDiv({ cls: "better-links-popover__form" });
    this.displayInputEl = formEl.createEl("input", {
      cls: "better-links-popover__input",
      attr: { type: "text", placeholder: this.t("popoverPlaceholderDisplay") }
    });
    const destinationRow = formEl.createDiv({ cls: "better-links-popover__input-row" });
    this.destinationInputEl = destinationRow.createEl("input", {
      cls: "better-links-popover__input",
      attr: { type: "text", placeholder: this.t("popoverPlaceholderDestination") }
    });
    this.destinationWarningEl = destinationRow.createSpan({
      cls: "better-links-popover__input-warning is-hidden",
      text: "\u26A0\uFE0F",
      attr: { "aria-label": this.t("popoverWarningTargetNotFound") }
    });
    this.destinationInputEl.addEventListener("input", () => {
      var _a, _b;
      (_b = (_a = this.events).onDestinationInput) == null ? void 0 : _b.call(_a, this.destinationInputEl.value);
    });
    const footerEl = this.rootEl.createDiv({ cls: "better-links-popover__footer" });
    const leftEl = footerEl.createDiv({ cls: "better-links-popover__footer-left" });
    const rightEl = footerEl.createDiv({ cls: "better-links-popover__footer-right" });
    this.embedButtonEl = this.createIconButton(leftEl, "file-braces", this.t("popoverAriaToggleEmbed"), () => {
      this.events.onToggleEmbed();
    });
    this.openButtonEl = this.createIconButton(leftEl, "external-link", this.t("popoverAriaOpen"), () => {
      this.events.onOpen(this.displayInputEl.value, this.destinationInputEl.value);
    });
    this.ctrlClickHintEl = leftEl.createSpan({
      cls: "better-links-popover__open-hint",
      text: this.t("popoverHintCtrlClick")
    });
    this.copyMarkdownButtonEl = this.createIconButton(rightEl, "copy", this.t("popoverAriaCopyMarkdown"), () => {
      this.events.onCopyMarkdown(this.displayInputEl.value, this.destinationInputEl.value);
    });
    this.copyUrlButtonEl = this.createIconButton(rightEl, "link", this.t("popoverAriaCopyUrl"), () => {
      this.events.onCopyUrl(this.destinationInputEl.value);
    });
    this.deleteButtonEl = this.createIconButton(rightEl, "trash-2", this.t("popoverAriaDelete"), (event) => {
      const forceRemoveAll = event.instanceOf(MouseEvent) && (event.ctrlKey || event.metaKey);
      this.events.onDelete(forceRemoveAll);
    }, true);
    this.forceSaveButtonEl = this.createIconButton(rightEl, "check", this.t("popoverAriaForceSave"), () => {
      this.events.onForceSave();
    });
    this.forceSaveButtonEl.toggleClass("is-hidden", true);
  }
  getValues() {
    return {
      displayText: this.displayInputEl.value,
      destination: this.destinationInputEl.value
    };
  }
  get destinationInput() {
    return this.destinationInputEl;
  }
  setDisplayText(value) {
    this.displayInputEl.value = value;
  }
  setSuggestActiveChecker(fn2) {
    this.isSuggestActiveChecker = fn2;
  }
  setDestinationWarning(hasWarning) {
    this.destinationInputEl.toggleClass("mod-warning", hasWarning);
    this.destinationWarningEl.toggleClass("is-hidden", !hasWarning);
    this.forceSaveButtonEl.toggleClass("is-hidden", !hasWarning);
  }
  /** 检查 popover 中是否有输入框获焦 */
  hasInputFocus() {
    return this.rootEl.contains(this.currentDoc.activeElement);
  }
  updateEmbedState(isEmbedded) {
    this.embedButtonEl.toggleClass("is-active", isEmbedded);
    const label = isEmbedded ? this.t("popoverAriaEmbedOn") : this.t("popoverAriaEmbedOff");
    this.embedButtonEl.setAttribute("aria-label", label);
  }
  isOpen() {
    return this.wrapperEl.isShown();
  }
  /** 检查某个节点是否在 popover 交互区域内（含外层 wrapper） */
  containsElement(node) {
    return this.wrapperEl.contains(node);
  }
  /** 获取外层交互容器用于事件绑定 */
  get rootElement() {
    return this.wrapperEl;
  }
  open(referenceEl, state, interactionEl) {
    var _a, _b, _c;
    const targetDoc = referenceEl instanceof Node ? referenceEl.doc : (_a = interactionEl == null ? void 0 : interactionEl.doc) != null ? _a : activeDocument;
    if (targetDoc !== this.currentDoc) {
      this.currentDoc = targetDoc;
      targetDoc.body.appendChild(this.wrapperEl);
    }
    this.typeBadgeEl.setText(state.typeLabel);
    this.displayInputEl.value = state.displayText;
    this.destinationInputEl.value = state.destination;
    this.destinationInputEl.removeClass("mod-warning");
    this.destinationWarningEl.toggleClass("is-hidden", true);
    this.forceSaveButtonEl.toggleClass("is-hidden", true);
    this.displayInputEl.placeholder = state.isImage ? this.t("popoverPlaceholderImageSize") : this.t("popoverPlaceholderDisplay");
    const copyUrlLabel2 = state.isImage ? this.t("popoverAriaCopyFileName") : this.t("popoverAriaCopyUrl");
    this.copyMarkdownButtonEl.setAttribute("aria-label", state.copyMarkdownLabel);
    this.copyUrlButtonEl.setAttribute("aria-label", state.copyUrlLabel || copyUrlLabel2);
    (0, import_obsidian2.setIcon)(this.copyUrlButtonEl, state.copyUrlIcon);
    this.deleteButtonEl.toggleClass("is-hidden", !state.showDelete);
    (0, import_obsidian2.setIcon)(this.openButtonEl, state.isInternal ? "file-symlink" : "external-link");
    this.ctrlClickHintEl.toggleClass("is-hidden", !state.showCtrlClickHint);
    this.embedButtonEl.toggleClass("is-hidden", !state.showEmbedToggle);
    this.updateEmbedState(state.isEmbedded);
    this.rootEl.removeClass("is-visible");
    this.wrapperEl.setCssStyles({ visibility: "hidden" });
    this.wrapperEl.show();
    (_b = this.popperInstance) == null ? void 0 : _b.destroy();
    this.popperInstance = createPopper(referenceEl, this.wrapperEl, {
      placement: "top-start",
      modifiers: [
        { name: "offset", options: { offset: [0, 6] } },
        { name: "flip", options: { fallbackPlacements: ["bottom-start", "top-end", "bottom-end"] } },
        { name: "preventOverflow", options: { padding: 8 } }
      ]
    });
    void this.popperInstance.update().then(() => {
      this.wrapperEl.setCssStyles({ visibility: "" });
      window.requestAnimationFrame(() => {
        this.rootEl.addClass("is-visible");
      });
    });
    this.attachGlobalListeners(interactionEl);
    const focusTarget = (_c = state.focusTarget) != null ? _c : "display";
    window.setTimeout(() => {
      if (focusTarget === "destination") {
        this.destinationInputEl.focus();
        this.destinationInputEl.select();
      } else {
        this.displayInputEl.focus();
        this.displayInputEl.select();
      }
    }, 0);
  }
  close() {
    var _a;
    this.detachGlobalListeners();
    this.rootEl.removeClass("is-visible");
    (_a = this.popperInstance) == null ? void 0 : _a.destroy();
    this.popperInstance = null;
    this.wrapperEl.hide();
  }
  destroy() {
    this.close();
    this.wrapperEl.remove();
  }
  createIconButton(parent, icon, ariaLabel, onClick, danger = false) {
    const btn = parent.createEl("button", {
      cls: `better-links-popover__icon-btn${danger ? " mod-danger" : ""}`,
      attr: { type: "button", "aria-label": ariaLabel }
    });
    (0, import_obsidian2.setIcon)(btn, icon);
    btn.addEventListener("click", onClick);
    return btn;
  }
  attachGlobalListeners(interactionEl) {
    var _a;
    this.detachGlobalListeners();
    this.outsidePointerDownHandler = (event) => {
      var _a2, _b;
      const target = event.target;
      if (!(target instanceof Node)) return;
      if (this.wrapperEl.contains(target) || (interactionEl == null ? void 0 : interactionEl.contains(target))) return;
      if (((_a2 = this.isSuggestActiveChecker) == null ? void 0 : _a2.call(this)) && ((_b = target.closest) == null ? void 0 : _b.call(target, ".suggestion-container"))) return;
      this.events.onClose();
    };
    this.keydownHandler = (event) => {
      var _a2;
      if ((_a2 = this.isSuggestActiveChecker) == null ? void 0 : _a2.call(this)) return;
      if (event.key === "Escape") {
        event.preventDefault();
        event.stopPropagation();
        this.events.onDiscard();
      } else if (event.key === "Enter") {
        event.preventDefault();
        event.stopPropagation();
        this.events.onClose();
      }
    };
    this.scrollHandler = () => {
      this.schedulePopperUpdate();
    };
    this.resizeHandler = () => {
      this.schedulePopperUpdate();
    };
    this.currentDoc.addEventListener("pointerdown", this.outsidePointerDownHandler, true);
    this.currentDoc.addEventListener("keydown", this.keydownHandler, true);
    this.currentDoc.addEventListener("scroll", this.scrollHandler, true);
    (_a = this.currentDoc.defaultView) == null ? void 0 : _a.addEventListener("resize", this.resizeHandler, true);
  }
  detachGlobalListeners() {
    var _a, _b, _c;
    if (this.outsidePointerDownHandler) {
      this.currentDoc.removeEventListener("pointerdown", this.outsidePointerDownHandler, true);
      this.outsidePointerDownHandler = null;
    }
    if (this.keydownHandler) {
      this.currentDoc.removeEventListener("keydown", this.keydownHandler, true);
      this.keydownHandler = null;
    }
    if (this.scrollHandler) {
      this.currentDoc.removeEventListener("scroll", this.scrollHandler, true);
      this.scrollHandler = null;
    }
    if (this.resizeHandler) {
      (_a = this.currentDoc.defaultView) == null ? void 0 : _a.removeEventListener("resize", this.resizeHandler, true);
      this.resizeHandler = null;
    }
    if (this.keydownHandler) {
      this.currentDoc.removeEventListener("keydown", this.keydownHandler, true);
      this.keydownHandler = null;
    }
    if (this.scrollHandler) {
      this.currentDoc.removeEventListener("scroll", this.scrollHandler, true);
      this.scrollHandler = null;
    }
    if (this.resizeHandler) {
      (_b = this.currentDoc.defaultView) == null ? void 0 : _b.removeEventListener("resize", this.resizeHandler, true);
      this.resizeHandler = null;
    }
    if (this.keydownHandler) {
      this.currentDoc.removeEventListener("keydown", this.keydownHandler, true);
      this.keydownHandler = null;
    }
    if (this.scrollHandler) {
      this.currentDoc.removeEventListener("scroll", this.scrollHandler, true);
      this.scrollHandler = null;
    }
    if (this.resizeHandler) {
      (_c = this.currentDoc.defaultView) == null ? void 0 : _c.removeEventListener("resize", this.resizeHandler, true);
      this.resizeHandler = null;
    }
    if (this.updateRafId !== null) {
      window.cancelAnimationFrame(this.updateRafId);
      this.updateRafId = null;
    }
  }
  schedulePopperUpdate() {
    if (!this.popperInstance || this.updateRafId !== null) return;
    this.updateRafId = window.requestAnimationFrame(() => {
      var _a;
      this.updateRafId = null;
      void ((_a = this.popperInstance) == null ? void 0 : _a.update());
    });
  }
};

// src/linkSuggest.ts
var import_obsidian3 = require("obsidian");
var MAX_SUGGESTIONS = 20;
var LinkDestinationSuggest = class extends import_obsidian3.AbstractInputSuggest {
  constructor(app, inputEl, sourcePath, callbacks, settings) {
    super(app, inputEl);
    this.inputEl = inputEl;
    this.callbacks = callbacks;
    this._active = false;
    /** 选中时临时屏蔽 input 事件触发 suggest，防止选中后下拉重开 */
    this._suppressNext = false;
    this.limit = MAX_SUGGESTIONS;
    this.sourcePath = sourcePath;
    this.settings = settings;
  }
  /** 每次打开 popover 时更新上下文，复用同一实例避免重复创建 suggestion-container */
  updateContext(sourcePath, settings) {
    this.sourcePath = sourcePath;
    this.settings = settings;
  }
  get isActive() {
    return this._active;
  }
  open() {
    this._active = true;
    super.open();
  }
  close() {
    this._active = false;
    super.close();
  }
  getSuggestions(query) {
    if (this._suppressNext) return [];
    const hashIndex = query.indexOf("#");
    if (hashIndex >= 0) {
      return this.getHeadingSuggestions(query, hashIndex);
    }
    return this.getFileSuggestions(query);
  }
  renderSuggestion(item, el) {
    if (item.kind === "file") {
      this.renderFileSuggestion(item, el);
    } else {
      this.renderHeadingSuggestion(item, el);
    }
  }
  selectSuggestion(item, _evt) {
    let destination;
    let subpath;
    const isSameFile = item.file.path === this.sourcePath;
    if (item.kind === "file") {
      destination = this.getDestinationPath(item.file);
    } else {
      destination = isSameFile ? "" : this.getDestinationPath(item.file);
      subpath = item.heading.heading;
    }
    const fullDestination = subpath ? `${destination}#${subpath}` : destination;
    this.setValue(fullDestination);
    const alias = this.computeAlias(item, isSameFile);
    if (alias !== null) {
      this.callbacks.setDisplayText(alias);
    }
    this._suppressNext = true;
    this.close();
    this.inputEl.dispatchEvent(new Event("input", { bubbles: true }));
    void Promise.resolve().then(() => {
      this._suppressNext = false;
    });
    this.callbacks.onSuggestionSelected();
  }
  // ── private: rendering ───────────────────────────────────────────────────
  renderFileSuggestion(item, el) {
    var _a, _b;
    const isMarkdown = item.file.extension === "md";
    const basenameDisplay = isMarkdown ? item.file.basename : item.file.name;
    const folder = (_b = (_a = item.file.parent) == null ? void 0 : _a.path) != null ? _b : "";
    if (!isMarkdown) {
      el.addClass("better-links-suggest__item--non-md");
    }
    const titleEl = el.createDiv({ cls: "better-links-suggest__title" });
    if (item.matchedAlias !== void 0) {
      el.addClass("better-links-suggest__item--alias");
      if (item.match && item.match.matches.length > 0) {
        (0, import_obsidian3.renderResults)(titleEl, item.matchedAlias, item.match);
      } else {
        titleEl.setText(item.matchedAlias);
      }
      const subText = folder && folder !== "/" ? `${basenameDisplay} \xB7 ${folder}` : basenameDisplay;
      el.createDiv({ cls: "better-links-suggest__path", text: subText });
      return;
    }
    if (item.match && item.match.matches.length > 0) {
      const basenameMatch = recomputeMatchForBasename(item.file.path, basenameDisplay, item.match);
      if (basenameMatch) {
        (0, import_obsidian3.renderResults)(titleEl, basenameDisplay, basenameMatch);
      } else {
        titleEl.setText(basenameDisplay);
      }
    } else {
      titleEl.setText(basenameDisplay);
    }
    if (folder && folder !== "/") {
      el.createDiv({ cls: "better-links-suggest__path", text: folder });
    }
  }
  renderHeadingSuggestion(item, el) {
    const prefix = "#".repeat(item.heading.level) + " ";
    const headingText = item.heading.heading;
    const titleEl = el.createDiv({ cls: "better-links-suggest__title" });
    const prefixSpan = titleEl.createSpan({ cls: "better-links-suggest__heading-prefix", text: prefix });
    prefixSpan.addClass("better-links-suggest__heading-prefix--dim");
    const textSpan = titleEl.createSpan();
    if (item.match && item.match.matches.length > 0) {
      (0, import_obsidian3.renderResults)(textSpan, headingText, item.match);
    } else {
      textSpan.setText(headingText);
    }
    el.createDiv({ cls: "better-links-suggest__path", text: item.file.basename });
  }
  // ── private: alias computation ───────────────────────────────────────────
  /**
   * 根据设置计算选中后应填入 displayText 的别名。
   * 返回 null 表示不修改 displayText。
   */
  computeAlias(item, isSameFile = false) {
    var _a, _b, _c;
    if (!((_a = this.settings.syncAlias) != null ? _a : false)) return null;
    const mode = (_b = this.settings.aliasSyncMode) != null ? _b : "heading-only";
    const sep = (_c = this.settings.aliasSeparator) != null ? _c : " > ";
    if (item.kind === "file") {
      if (item.matchedAlias !== void 0) {
        return item.matchedAlias;
      }
      const fileName2 = this.getDisplayName(item.file);
      return fileName2;
    }
    const headingText = item.heading.heading;
    if (isSameFile) {
      return headingText;
    }
    const fileName = this.getDisplayName(item.file);
    switch (mode) {
      case "heading-only":
        return headingText;
      case "filename-then-heading":
        return `${fileName}${sep}${headingText}`;
      case "heading-then-filename":
        return `${headingText}${sep}${fileName}`;
    }
  }
  /**
   * 获取文件的展示名：优先读 frontmatter 中指定属性，fallback 到 basename。
   */
  getDisplayName(file) {
    var _a, _b;
    const propertyKey = (_a = this.settings.aliasTitleProperty) != null ? _a : "title";
    const fm = (_b = this.app.metadataCache.getFileCache(file)) == null ? void 0 : _b.frontmatter;
    const titleFromFm = fm == null ? void 0 : fm[propertyKey];
    if (typeof titleFromFm === "string" && titleFromFm.trim()) {
      return titleFromFm.trim();
    }
    return file.basename;
  }
  // ── private: destination path ────────────────────────────────────────────
  /**
   * 生成遵循 OB 内部链接格式设置的 destination 路径字符串。
   * 使用 generateMarkdownLink 生成完整链接，再从中提取路径部分。
   */
  getDestinationPath(file) {
    var _a, _b;
    const fullLink = this.app.fileManager.generateMarkdownLink(file, this.sourcePath);
    const wikiMatch = /^\[\[([^\]|]+)(?:\|[^\]]+)?\]\]$/.exec(fullLink);
    if (wikiMatch) {
      return (_a = wikiMatch[1]) != null ? _a : file.path;
    }
    const mdMatch = /^\[.*?\]\(([^)]+)\)$/.exec(fullLink);
    if (mdMatch) {
      return (_b = mdMatch[1]) != null ? _b : file.path;
    }
    return file.path;
  }
  // ── private: suggestions ─────────────────────────────────────────────────
  getFileSuggestions(query) {
    const files = this.app.vault.getFiles();
    if (!query) {
      return files.slice().sort((a, b) => a.path.localeCompare(b.path)).slice(0, MAX_SUGGESTIONS).map((file) => ({ kind: "file", file, match: null }));
    }
    const search = (0, import_obsidian3.prepareFuzzySearch)(query);
    const results = [];
    for (const file of files) {
      const matchPath = search(file.path);
      const matchBasename = search(file.basename);
      const nameMatch = betterMatch(matchPath, matchBasename);
      const aliasHit = this.findBestAliasMatch(file, search);
      if (aliasHit && (!nameMatch || aliasHit.match.score > nameMatch.score)) {
        results.push({
          kind: "file",
          file,
          match: aliasHit.match,
          matchedAlias: aliasHit.alias
        });
      } else if (nameMatch) {
        results.push({ kind: "file", file, match: nameMatch });
      }
    }
    results.sort((a, b) => {
      var _a, _b, _c, _d;
      return ((_b = (_a = b.match) == null ? void 0 : _a.score) != null ? _b : 0) - ((_d = (_c = a.match) == null ? void 0 : _c.score) != null ? _d : 0);
    });
    return results.slice(0, MAX_SUGGESTIONS);
  }
  /**
   * 从文件 frontmatter.aliases 中找出与 query 最佳匹配的别名。
   * aliases 字段允许是 string 或 string[]（OB 标准）。
   */
  findBestAliasMatch(file, search) {
    var _a;
    const aliases = readAliases((_a = this.app.metadataCache.getFileCache(file)) == null ? void 0 : _a.frontmatter);
    if (aliases.length === 0) return null;
    let best = null;
    for (const alias of aliases) {
      const m = search(alias);
      if (!m) continue;
      if (!best || m.score > best.match.score) {
        best = { alias, match: m };
      }
    }
    return best;
  }
  getHeadingSuggestions(query, hashIndex) {
    var _a, _b;
    const filePart = query.slice(0, hashIndex);
    const headingQuery = query.slice(hashIndex + 1);
    const file = this.app.metadataCache.getFirstLinkpathDest(filePart, this.sourcePath);
    if (!file) return [];
    const headings = (_b = (_a = this.app.metadataCache.getFileCache(file)) == null ? void 0 : _a.headings) != null ? _b : [];
    if (headings.length === 0) return [];
    if (!headingQuery) {
      return headings.slice(0, MAX_SUGGESTIONS).map((heading) => ({
        kind: "heading",
        file,
        heading,
        filePart,
        match: null
      }));
    }
    const search = (0, import_obsidian3.prepareFuzzySearch)(headingQuery);
    const results = [];
    for (const heading of headings) {
      const match = search(heading.heading);
      if (match) {
        results.push({ kind: "heading", file, heading, filePart, match });
      }
    }
    results.sort((a, b) => {
      var _a2, _b2, _c, _d;
      return ((_b2 = (_a2 = b.match) == null ? void 0 : _a2.score) != null ? _b2 : 0) - ((_d = (_c = a.match) == null ? void 0 : _c.score) != null ? _d : 0);
    });
    return results.slice(0, MAX_SUGGESTIONS);
  }
};
function betterMatch(a, b) {
  if (!a && !b) return null;
  if (!a) return b;
  if (!b) return a;
  return a.score >= b.score ? a : b;
}
function recomputeMatchForBasename(fullPath, basename, result) {
  const offset2 = fullPath.length - basename.length;
  if (offset2 < 0) return null;
  const shifted = result.matches.map(([start2, end2]) => [start2 - offset2, end2 - offset2]).filter(([start2, end2]) => start2 >= 0 && end2 <= basename.length);
  if (shifted.length === 0) return null;
  return { score: result.score, matches: shifted };
}
function readAliases(frontmatter) {
  var _a;
  if (!frontmatter) return [];
  const raw = (_a = frontmatter.aliases) != null ? _a : frontmatter.alias;
  const out = [];
  if (typeof raw === "string") {
    const v = raw.trim();
    if (v) out.push(v);
  } else if (Array.isArray(raw)) {
    for (const item of raw) {
      if (typeof item === "string") {
        const v = item.trim();
        if (v) out.push(v);
      }
    }
  }
  return out;
}

// src/linkEditManager.ts
var LinkEditManager = class {
  constructor(plugin) {
    this.plugin = plugin;
    this.activeSession = null;
    /** 当前 destination 输入框是否处于警告状态（校验失败） */
    this.destinationInvalid = false;
    /** debounce 定时器 */
    this.validateDebounceTimer = null;
    this.popoverEditor = new PopoverEditor({
      onSave: (displayText, destination) => {
        this.save(displayText, destination);
      },
      onOpen: (displayText, destination) => {
        void this.open(displayText, destination);
      },
      onCopyMarkdown: (displayText, destination) => {
        void this.copyMarkdown(displayText, destination);
      },
      onCopyUrl: (destination) => {
        const session = this.activeSession;
        if (!session) {
          return;
        }
        void copyUrl(this.plugin.app, session.match, destination);
      },
      onDelete: (forceRemoveAll) => {
        this.deleteCurrentLink(forceRemoveAll);
      },
      onToggleEmbed: () => {
        this.toggleEmbed();
      },
      onClose: () => {
        this.saveAndClose();
      },
      onForceSave: () => {
        this.forceSaveAndClose();
      },
      onDiscard: () => {
        this.discardAndClose();
      },
      onDestinationInput: (destination) => {
        this.scheduleValidation(destination);
      }
    }, this.plugin.t.bind(this.plugin));
    this.suggest = new LinkDestinationSuggest(
      this.plugin.app,
      this.popoverEditor.destinationInput,
      "",
      {
        setDisplayText: (value) => {
          this.popoverEditor.setDisplayText(value);
        },
        onSuggestionSelected: () => {
          this.cancelPendingValidation();
          this.setWarning(false);
        }
      },
      this.plugin.settings
    );
    this.popoverEditor.setSuggestActiveChecker(() => this.suggest.isActive);
  }
  isOpen() {
    return this.popoverEditor.isOpen();
  }
  /** 检查某个 DOM 节点是否在 popover 区域内 */
  isMouseOverPopover(node) {
    return this.popoverEditor.containsElement(node);
  }
  /** 获取 popover 根元素用于外部事件绑定 */
  get popoverRootElement() {
    return this.popoverEditor.rootElement;
  }
  /** 用户是否正在与 popover 交互（输入框获焦或 suggest 下拉打开） */
  isUserInteracting() {
    return this.popoverEditor.hasInputFocus() || this.suggest.isActive;
  }
  show(match, referenceEl, interactionEl) {
    var _a, _b, _c, _d;
    this.cancelPendingValidation();
    this.suggest.close();
    this.destinationInvalid = false;
    this.activeSession = {
      match,
      // 编辑现有链接：默认把光标放到链接末尾
      cursorOnClose: { ...match.range.to },
      isNew: false
    };
    const isImage = match.type === "imageWiki" || match.type === "imageMarkdown";
    const showEmbedToggle = !!this.plugin.settings.showEmbedToggle && canToggleEmbed(match);
    const isEmbedded = match.originalText.startsWith("!");
    const showCtrlClickHint = ((_a = this.plugin.settings.triggerMethod) != null ? _a : "hover") === "click" && ((_b = this.plugin.settings.triggerModifier) != null ? _b : "none") === "none";
    const displayText = ((_c = this.plugin.settings.alwaysShowDisplayText) != null ? _c : false) || match.hasExplicitDisplayText ? match.displayText : "";
    const isInternal = match.type === "wiki" || match.type === "imageWiki" || match.type === "markdown" && isLikelyInternalDestination(match.destination) || match.type === "imageMarkdown" && isLikelyInternalDestination(match.destination);
    this.popoverEditor.open(referenceEl, {
      displayText,
      destination: match.destination,
      typeLabel: linkTypeLabel(match.type, this.plugin),
      isImage,
      isInternal,
      copyMarkdownLabel: copyMarkdownLabel(match, this.plugin),
      copyUrlLabel: copyUrlLabel(match, this.plugin),
      copyUrlIcon: copyUrlIcon(match),
      showDelete: !isImage,
      showEmbedToggle,
      isEmbedded,
      showCtrlClickHint
    }, interactionEl);
    const shouldSuggest = ((_d = this.plugin.settings.enableLinkSuggestions) != null ? _d : true) && (match.type === "wiki" || match.type === "markdown");
    if (shouldSuggest) {
      this.suggest.updateContext(match.sourcePath, this.plugin.settings);
    }
  }
  /**
   * 命令触发：在当前光标位置弹出浮窗。
   * - 光标处有链接 → 弹出编辑该链接的 popup（等同点击链接效果）
   * - 光标处无链接 → 弹出新建链接的 popup，聚焦"URL 或笔记路径"输入框
   *
   * @returns 是否成功弹出（无活动编辑器时返回 false）
   */
  showAtCursor() {
    var _a, _b, _c;
    const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView);
    if (!(markdownView == null ? void 0 : markdownView.file)) {
      new import_obsidian4.Notice(this.plugin.t("noticeNoActiveEditor"));
      return false;
    }
    const editor = markdownView.editor;
    const cursor = editor.getCursor();
    const cmEditorEl = markdownView.containerEl.querySelector(".cm-editor");
    if (!(cmEditorEl instanceof HTMLElement)) {
      new import_obsidian4.Notice(this.plugin.t("noticeNoActiveEditor"));
      return false;
    }
    const editorView = import_view.EditorView.findFromDOM(cmEditorEl);
    if (!editorView) {
      new import_obsidian4.Notice(this.plugin.t("noticeNoActiveEditor"));
      return false;
    }
    const lineFrom = editorView.state.doc.line(cursor.line + 1).from;
    const cursorOffset = lineFrom + cursor.ch;
    const referenceEl = createCursorVirtualReference(editorView, cursorOffset);
    const lineText = editor.getLine(cursor.line);
    const relMatch = findLinkAtOffset(lineText, cursor.ch, this.plugin.settings, true);
    if (relMatch) {
      const editorMatch = withEditorRange(relMatch, cursor.line, markdownView.file.path);
      this.show(editorMatch, referenceEl);
      return true;
    }
    const useWiki = shouldUseWikiLinkFormat(this.plugin.app);
    const placeholder = {
      type: useWiki ? "wiki" : "markdown",
      start: cursor.ch,
      end: cursor.ch,
      originalText: "",
      displayText: "",
      destination: "",
      hasExplicitDisplayText: false,
      range: {
        from: { line: cursor.line, ch: cursor.ch },
        to: { line: cursor.line, ch: cursor.ch }
      },
      sourcePath: markdownView.file.path
    };
    this.cancelPendingValidation();
    this.suggest.close();
    this.destinationInvalid = false;
    this.activeSession = {
      match: placeholder,
      cursorOnClose: { line: cursor.line, ch: cursor.ch },
      isNew: true
    };
    const showCtrlClickHint = ((_a = this.plugin.settings.triggerMethod) != null ? _a : "hover") === "click" && ((_b = this.plugin.settings.triggerModifier) != null ? _b : "none") === "none";
    const showEmbedToggle = !!this.plugin.settings.showEmbedToggle && canToggleEmbed(placeholder);
    this.popoverEditor.open(referenceEl, {
      displayText: "",
      destination: "",
      typeLabel: linkTypeLabel(placeholder.type, this.plugin),
      isImage: false,
      isInternal: useWiki,
      copyMarkdownLabel: copyMarkdownLabel(placeholder, this.plugin),
      copyUrlLabel: copyUrlLabel(placeholder, this.plugin),
      copyUrlIcon: copyUrlIcon(placeholder),
      showDelete: false,
      showEmbedToggle,
      isEmbedded: false,
      showCtrlClickHint,
      focusTarget: "destination"
    });
    if ((_c = this.plugin.settings.enableLinkSuggestions) != null ? _c : true) {
      this.suggest.updateContext(placeholder.sourcePath, this.plugin.settings);
    }
    return true;
  }
  close() {
    this.saveAndClose();
  }
  destroy() {
    this.cancelPendingValidation();
    this.suggest.close();
    this.activeSession = null;
    this.popoverEditor.destroy();
  }
  /** Auto-save current edits then close the popover. */
  saveAndClose() {
    var _a, _b;
    const cursorTarget = this.activeSession ? { ...this.activeSession.cursorOnClose } : null;
    if (this.activeSession && this.popoverEditor.isOpen()) {
      const { displayText, destination } = this.popoverEditor.getValues();
      const silent = !this.destinationInvalid;
      this.save(displayText, destination, silent);
    }
    const finalCursor = (_b = (_a = this.activeSession) == null ? void 0 : _a.cursorOnClose) != null ? _b : cursorTarget;
    this.cancelPendingValidation();
    this.closeSuggest();
    this.destinationInvalid = false;
    this.activeSession = null;
    this.popoverEditor.close();
    if (finalCursor) {
      this.restoreEditorFocus(finalCursor);
    }
  }
  /** 强制保存（跳过校验）并关闭。 */
  forceSaveAndClose() {
    var _a, _b;
    const cursorTarget = this.activeSession ? { ...this.activeSession.cursorOnClose } : null;
    if (this.activeSession && this.popoverEditor.isOpen()) {
      const { displayText, destination } = this.popoverEditor.getValues();
      this.destinationInvalid = false;
      this.save(displayText, destination, true);
    }
    const finalCursor = (_b = (_a = this.activeSession) == null ? void 0 : _a.cursorOnClose) != null ? _b : cursorTarget;
    this.cancelPendingValidation();
    this.closeSuggest();
    this.destinationInvalid = false;
    this.activeSession = null;
    this.popoverEditor.close();
    if (finalCursor) {
      this.restoreEditorFocus(finalCursor);
    }
  }
  /** 丢弃编辑，直接关闭（ESC 触发）。 */
  discardAndClose() {
    const finalCursor = this.activeSession ? { ...this.activeSession.cursorOnClose } : null;
    this.cancelPendingValidation();
    this.closeSuggest();
    this.destinationInvalid = false;
    this.activeSession = null;
    this.popoverEditor.close();
    if (finalCursor) {
      this.restoreEditorFocus(finalCursor);
    }
  }
  /** 关闭浮窗后把焦点交还给编辑器并把光标放在指定位置，避免打断编辑流。 */
  restoreEditorFocus(cursor) {
    const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView);
    const editor = markdownView == null ? void 0 : markdownView.editor;
    if (!editor) return;
    editor.focus();
    editor.setCursor(cursor);
  }
  save(displayText, destination, silent = false) {
    const session = this.activeSession;
    if (!session) return;
    if (this.destinationInvalid) {
      if (!silent) {
        new import_obsidian4.Notice(this.plugin.t("noticeInternalLinkNotFound"));
      }
      return;
    }
    if (session.isNew && destination.trim().length === 0) {
      return;
    }
    const nextText = serializeEditedLink(session.match, displayText, destination, {
      preferWikiLink: shouldUseWikiLinkFormat(this.plugin.app)
    });
    if (nextText === session.match.originalText) return;
    let replacement = nextText;
    if (session.isNew) {
      const padded = padIfNewLink(nextText, session.match.range.from, this.plugin);
      replacement = padded.text;
      session.cursorOnClose = {
        line: session.match.range.from.line,
        ch: session.match.range.from.ch + padded.cursorOffsetWithin
      };
    } else {
      session.cursorOnClose = {
        line: session.match.range.from.line,
        ch: session.match.range.from.ch + replacement.length
      };
    }
    session.match.destination = destination.trim();
    session.match.displayText = displayText.trim();
    session.match.originalText = nextText;
    this.replaceActiveRange(replacement);
    if (!silent) {
      new import_obsidian4.Notice(this.plugin.t("noticeLinkUpdated"));
    }
  }
  async open(displayText, destination) {
    const session = this.activeSession;
    if (!session) {
      return;
    }
    const values = normalizeEditableValues(session.match, displayText, destination);
    await openLink(this.plugin.app, session.match, values, this.plugin.settings);
    this.close();
  }
  async copyMarkdown(displayText, destination) {
    const session = this.activeSession;
    if (!session) {
      return;
    }
    const values = normalizeEditableValues(session.match, displayText, destination);
    await copyMarkdown(this.plugin.app, session.match, values);
  }
  deleteCurrentLink(forceRemoveAll = false) {
    const session = this.activeSession;
    if (!session) {
      return;
    }
    const isPlainUrl = session.match.type === "url";
    const removeAll = forceRemoveAll || isPlainUrl;
    let replacement;
    let noticeKey;
    if (removeAll) {
      replacement = "";
      noticeKey = "noticeLinkRemovedAll";
    } else {
      replacement = buildDeletionText(session.match, this.plugin.settings);
      noticeKey = "noticeLinkRemoved";
    }
    const cursorPos = removeAll ? { ...session.match.range.from } : null;
    this.replaceActiveRange(replacement);
    if (cursorPos) {
      const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView);
      const editor = markdownView == null ? void 0 : markdownView.editor;
      if (editor) {
        editor.focus();
        editor.setCursor(cursorPos);
      }
    }
    new import_obsidian4.Notice(this.plugin.t(noticeKey));
    this.close();
  }
  /** 切换当前链接的 ! 前缀（嵌入/非嵌入） */
  toggleEmbed() {
    const session = this.activeSession;
    if (!session) return;
    const { displayText, destination } = this.popoverEditor.getValues();
    const isCurrentlyEmbedded = session.match.originalText.startsWith("!");
    const nextEmbedded = !isCurrentlyEmbedded;
    const nextText = rebuildWithEmbedPrefix(session.match, displayText, destination, nextEmbedded);
    if (nextText === session.match.originalText) return;
    session.match.originalText = nextText;
    this.replaceActiveRange(nextText);
    this.popoverEditor.updateEmbedState(nextEmbedded);
  }
  replaceActiveRange(replacement) {
    const session = this.activeSession;
    if (!session) {
      return;
    }
    const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView);
    const editor = markdownView == null ? void 0 : markdownView.editor;
    if (!editor) {
      new import_obsidian4.Notice(this.plugin.t("noticeNoActiveEditor"));
      return;
    }
    editor.replaceRange(replacement, session.match.range.from, session.match.range.to, "better-links");
    session.match.range.to = {
      line: session.match.range.from.line,
      ch: session.match.range.from.ch + replacement.length
    };
  }
  /** 调度 debounce 校验（300ms） */
  scheduleValidation(destination) {
    this.cancelPendingValidation();
    this.validateDebounceTimer = window.setTimeout(() => {
      this.validateDestination(destination);
    }, 300);
  }
  cancelPendingValidation() {
    if (this.validateDebounceTimer !== null) {
      window.clearTimeout(this.validateDebounceTimer);
      this.validateDebounceTimer = null;
    }
  }
  closeSuggest() {
    this.suggest.close();
  }
  /** 校验内部链接目标是否存在 */
  validateDestination(destination) {
    var _a;
    const session = this.activeSession;
    if (!session) return;
    const isInternal = session.match.type === "wiki" || session.match.type === "imageWiki" || session.match.type === "imageMarkdown" || session.match.type === "markdown";
    if (!isInternal) return;
    if (!((_a = this.plugin.settings.validateInternalLinks) != null ? _a : true)) return;
    const trimmed = destination.trim();
    if (!trimmed) {
      this.setWarning(false);
      return;
    }
    if (isLikelyExternalDestination(trimmed)) {
      this.setWarning(false);
      return;
    }
    const valid = this.checkInternalTarget(trimmed, session.match.sourcePath);
    this.setWarning(!valid);
  }
  /**
   * 检查内部链接目标是否存在。
   * 支持格式：
   *  - `note`             纯文件名
   *  - `folder/note`      路径
   *  - `note#heading`     文件内标题
   *  - `#heading`         当前文件内标题
   *  - `note.png` 等图片扩展名
   */
  checkInternalTarget(destination, sourcePath) {
    const hashIndex = destination.indexOf("#");
    const filePart = hashIndex >= 0 ? destination.slice(0, hashIndex) : destination;
    const headingPart = hashIndex >= 0 ? destination.slice(hashIndex + 1) : null;
    if (filePart === "" && headingPart !== null) {
      const currentFile = this.plugin.app.vault.getAbstractFileByPath(sourcePath);
      if (!currentFile || !("stat" in currentFile)) return false;
      return this.headingExistsInFile(currentFile, headingPart);
    }
    const resolvedFile = this.plugin.app.metadataCache.getFirstLinkpathDest(filePart, sourcePath);
    if (!resolvedFile) return false;
    if (headingPart === null || headingPart === "") return true;
    return this.headingExistsInFile(resolvedFile, headingPart);
  }
  headingExistsInFile(file, heading) {
    const cache = this.plugin.app.metadataCache.getFileCache(file);
    if (!(cache == null ? void 0 : cache.headings)) return false;
    const normalized = heading.toLowerCase();
    return cache.headings.some((h) => h.heading.toLowerCase() === normalized);
  }
  setWarning(hasWarning) {
    this.destinationInvalid = hasWarning;
    this.popoverEditor.setDestinationWarning(hasWarning);
  }
};
function linkTypeLabel(type, plugin) {
  if (type === "wiki") {
    return plugin.t("typeLabelWiki");
  }
  if (type === "markdown") {
    return plugin.t("typeLabelMarkdown");
  }
  if (type === "imageWiki") {
    return plugin.t("typeLabelImageWiki");
  }
  if (type === "imageMarkdown") {
    return plugin.t("typeLabelImageMarkdown");
  }
  return plugin.t("typeLabelUrl");
}
function copyMarkdownLabel(match, plugin) {
  if (isLikelyExternalDestination(match.destination)) {
    return plugin.t("popoverAriaCopyMarkdown");
  }
  return shouldUseWikiLinkFormat(plugin.app) ? plugin.t("popoverAriaCopyWikiLink") : plugin.t("popoverAriaCopyMarkdown");
}
function copyUrlLabel(match, plugin) {
  if (match.type === "imageWiki" || match.type === "imageMarkdown") {
    return plugin.t("popoverAriaCopyFileName");
  }
  return plugin.t("popoverAriaCopyUrl");
}
function copyUrlIcon(_match) {
  return "link";
}
function canToggleEmbed(match) {
  return match.type === "wiki" || match.type === "markdown" || match.type === "imageWiki" || match.type === "imageMarkdown";
}
function rebuildWithEmbedPrefix(match, displayText, destination, embed) {
  const prefix = embed ? "!" : "";
  const dest = destination.trim();
  const disp = displayText.trim();
  if (match.type === "wiki" || match.type === "imageWiki") {
    if (!dest) return "";
    const hasAlias = disp.length > 0 && disp !== dest;
    return hasAlias ? `${prefix}[[${dest}|${disp}]]` : `${prefix}[[${dest}]]`;
  }
  return `${prefix}[${disp}](${dest})`;
}
function createCursorVirtualReference(editorView, offset2) {
  let lastRect = new DOMRect(0, 0, 1, 1);
  return {
    contextElement: editorView.dom,
    getBoundingClientRect: () => {
      const pos = editorView.coordsAtPos(offset2);
      if (pos) {
        lastRect = new DOMRect(pos.left, pos.top, 1, Math.max(1, pos.bottom - pos.top));
      }
      return lastRect;
    }
  };
}
function padIfNewLink(linkText, insertAt, plugin) {
  var _a;
  const enabled = (_a = plugin.settings.padNewLinkWithSpaces) != null ? _a : true;
  if (!enabled) {
    return { text: linkText, cursorOffsetWithin: linkText.length };
  }
  const markdownView = plugin.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView);
  const editor = markdownView == null ? void 0 : markdownView.editor;
  if (!editor) {
    return { text: linkText, cursorOffsetWithin: linkText.length };
  }
  const lineText = editor.getLine(insertAt.line);
  const charBefore = insertAt.ch > 0 ? lineText.charAt(insertAt.ch - 1) : "";
  const charAfter = insertAt.ch < lineText.length ? lineText.charAt(insertAt.ch) : "";
  const leftPad = charBefore && !isAdjacentBoundaryChar(charBefore) ? " " : "";
  const rightPad = charAfter && !isAdjacentBoundaryChar(charAfter) ? " " : "";
  const text = `${leftPad}${linkText}${rightPad}`;
  return { text, cursorOffsetWithin: text.length };
}
function isAdjacentBoundaryChar(ch) {
  if (/\s/.test(ch)) return true;
  if (/[.,;:!?\-)\]}>"'`/]/.test(ch)) return true;
  if (/[，。；：！？、）】》」』""'']/.test(ch)) return true;
  return false;
}

// src/linkInterceptor.ts
var import_view2 = require("@codemirror/view");
var import_obsidian5 = require("obsidian");
var HOVER_DELAY = 300;
var HOVER_LEAVE_DELAY = 500;
function parseExcludeKeywords(raw) {
  if (!raw) return [];
  return raw.split(/[,\n]/).map((k) => k.trim().toLowerCase()).filter((k) => k.length > 0);
}
function matchesExcludeKeyword(destination, keywords) {
  if (keywords.length === 0) return false;
  const lower = destination.toLowerCase();
  return keywords.some((kw) => lower.includes(kw));
}
function dbg(enabled, ...args) {
  if (enabled) console.debug("[BetterLinks]", ...args);
}
function computeVisibleEndOffset(match, lineStartOffset) {
  if (match.type === "markdown" || match.type === "imageMarkdown") {
    const closeBracket = match.originalText.indexOf("]");
    if (closeBracket >= 0) {
      return lineStartOffset + match.start + closeBracket + 1;
    }
  }
  return lineStartOffset + match.end;
}
function computeVisibleEndCh(match) {
  if (match.type === "markdown" || match.type === "imageMarkdown") {
    const closeBracket = match.originalText.indexOf("]");
    if (closeBracket >= 0) {
      return match.range.from.ch + closeBracket + 1;
    }
  }
  return match.range.to.ch;
}
var LinkInterceptor = class {
  constructor(plugin, linkEditManager) {
    this.plugin = plugin;
    this.linkEditManager = linkEditManager;
    /** hover 模式：延迟显示定时器 */
    this.hoverShowTimer = null;
    /** hover 模式：延迟关闭定时器 */
    this.hoverHideTimer = null;
    /** hover 模式：当前悬停链接的序列化 key（用于判断是否同一个链接） */
    this.hoveredLinkKey = null;
    /** pointerdown 拦截成功标记，用于让后续 click 事件配合 preventDefault */
    this.pointerDownIntercepted = false;
    /** touchstart 阶段记录的起始坐标，用于 touchmove 判断是否为拖动 */
    this.touchStartX = 0;
    this.touchStartY = 0;
    /** touchstart 确认了链接，等待 touchend 打开 popover */
    this.touchIntercepted = false;
    /** touchstart 阶段保存的链接匹配和上下文，供 touchend 使用 */
    this.touchLinkContext = null;
  }
  /**
   * pointerdown capture：在移动端 Obsidian 处理链接跳转之前抢先拦截。
   *
   * click 触发模式：拦截左键（button === 0）和 touch，标记此次交互已被插件接管，
   * 后续 click 事件中根据标记完成打开 popover 的实际逻辑。
   *
   * right-click 触发模式：拦截右键（button === 2）的 pointerdown，
   * preventDefault 阻止后续 mousedown 事件传播给 CM6，
   * 防止光标移动导致 Live Preview 展开链接（文本跳动）。
   */
  handlePointerDown(event) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x;
    this.pointerDownIntercepted = false;
    dbg((_a = this.plugin.settings.debugMode) != null ? _a : false, "pointerdown", { pointerType: event.pointerType, button: event.button, clientX: event.clientX, clientY: event.clientY });
    if (!this.plugin.settings.enabled) {
      dbg((_b = this.plugin.settings.debugMode) != null ? _b : false, "\u23F9 disabled");
      return;
    }
    const triggerMethod = (_c = this.plugin.settings.triggerMethod) != null ? _c : "hover";
    dbg((_d = this.plugin.settings.debugMode) != null ? _d : false, "triggerMethod:", triggerMethod);
    if (triggerMethod === "right-click" && event.button === 2) {
      this.preventRightClickExpansion(event);
      return;
    }
    if (triggerMethod !== "click") {
      dbg((_e = this.plugin.settings.debugMode) != null ? _e : false, "\u23F9 not click mode, skip pointerdown intercept");
      return;
    }
    if (event.button > 0) {
      dbg((_f = this.plugin.settings.debugMode) != null ? _f : false, "\u23F9 non-left button:", event.button);
      return;
    }
    const target = event.target;
    if (!(target instanceof HTMLElement)) {
      dbg((_g = this.plugin.settings.debugMode) != null ? _g : false, "\u23F9 target not HTMLElement");
      return;
    }
    if (target.matches("img") || target.closest("img") || target.closest(".image-embed")) {
      dbg((_h = this.plugin.settings.debugMode) != null ? _h : false, "\u23F9 image target");
      return;
    }
    const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian5.MarkdownView);
    if (!(markdownView == null ? void 0 : markdownView.file)) {
      dbg((_i = this.plugin.settings.debugMode) != null ? _i : false, "\u23F9 no active MarkdownView/file");
      return;
    }
    if (!markdownView.containerEl.contains(target)) {
      dbg((_j = this.plugin.settings.debugMode) != null ? _j : false, "\u23F9 target not inside MarkdownView");
      return;
    }
    const cmEditorEl = target.closest(".cm-editor");
    if (!(cmEditorEl instanceof HTMLElement)) {
      dbg((_k = this.plugin.settings.debugMode) != null ? _k : false, "\u23F9 no .cm-editor ancestor");
      return;
    }
    const editorView = import_view2.EditorView.findFromDOM(cmEditorEl);
    if (!editorView) {
      dbg((_l = this.plugin.settings.debugMode) != null ? _l : false, "\u23F9 EditorView.findFromDOM returned null");
      return;
    }
    const rect = target.getBoundingClientRect();
    const x = event.clientX || rect.left + rect.width / 2;
    const y = event.clientY || rect.top + rect.height / 2;
    dbg((_m = this.plugin.settings.debugMode) != null ? _m : false, "coords used:", { x, y, rawClientX: event.clientX, rawClientY: event.clientY });
    const documentOffset = editorView.posAtCoords({ x, y }, false);
    dbg((_n = this.plugin.settings.debugMode) != null ? _n : false, "documentOffset:", documentOffset);
    if (documentOffset == null) {
      dbg((_o = this.plugin.settings.debugMode) != null ? _o : false, "\u23F9 posAtCoords returned null");
      return;
    }
    const position = markdownView.editor.offsetToPos(documentOffset);
    const lineText = markdownView.editor.getLine(position.line);
    dbg((_p = this.plugin.settings.debugMode) != null ? _p : false, "line", position.line, "ch", position.ch, JSON.stringify(lineText));
    const match = findLinkAtOffset(lineText, position.ch, this.plugin.settings);
    dbg((_q = this.plugin.settings.debugMode) != null ? _q : false, "findLinkAtOffset result:", match);
    if (!match) {
      dbg((_r = this.plugin.settings.debugMode) != null ? _r : false, "\u23F9 no link match at offset");
      return;
    }
    if ((_s = this.plugin.settings.edgeProtection) != null ? _s : true) {
      const lineStartOffset = editorView.state.doc.line(position.line + 1).from;
      const matchStartOffset = lineStartOffset + match.start;
      const matchStart = editorView.coordsAtPos(matchStartOffset);
      const matchEnd = editorView.coordsAtPos(lineStartOffset + match.end);
      dbg((_t = this.plugin.settings.debugMode) != null ? _t : false, "edgeProtection check:", { matchStart, matchEnd, x, y });
      if (matchStart && matchEnd) {
        const inside = this.isPointInsideLinkRect(x, y, documentOffset, matchStartOffset, lineStartOffset + match.end, matchStart, matchEnd);
        dbg((_u = this.plugin.settings.debugMode) != null ? _u : false, "isPointInsideLinkRect:", inside);
        if (!inside) {
          dbg((_v = this.plugin.settings.debugMode) != null ? _v : false, "\u23F9 edge protection rejected");
          return;
        }
      }
    }
    if (event.pointerType === "touch") {
      dbg((_w = this.plugin.settings.debugMode) != null ? _w : false, "\u2705 pointerdown intercepted (touch \u2014 preventDefault only, no stopPropagation)");
      event.preventDefault();
    } else {
      dbg((_x = this.plugin.settings.debugMode) != null ? _x : false, "\u2705 pointerdown intercepted (mouse \u2014 preventDefault + stopPropagation)");
      event.preventDefault();
      event.stopPropagation();
    }
    this.pointerDownIntercepted = true;
  }
  /**
   * right-click 模式专用：在 pointerdown(button=2) 阶段检测右键是否落在链接上，
   * 如果是则 preventDefault 阻止后续 mousedown 传播给 CM6，
   * 防止光标移动导致 Live Preview 展开/折叠（文本跳动）。
   */
  preventRightClickExpansion(event) {
    var _a;
    const target = event.target;
    if (!(target instanceof HTMLElement)) return;
    if (target.matches("img") || target.closest("img") || target.closest(".image-embed")) return;
    const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian5.MarkdownView);
    if (!(markdownView == null ? void 0 : markdownView.file) || !markdownView.containerEl.contains(target)) return;
    const cmEditorEl = target.closest(".cm-editor");
    if (!(cmEditorEl instanceof HTMLElement)) return;
    const editorView = import_view2.EditorView.findFromDOM(cmEditorEl);
    if (!editorView) return;
    const documentOffset = editorView.posAtCoords({ x: event.clientX, y: event.clientY }, false);
    if (documentOffset == null) return;
    const position = markdownView.editor.offsetToPos(documentOffset);
    const lineText = markdownView.editor.getLine(position.line);
    const match = findLinkAtOffset(lineText, position.ch, this.plugin.settings);
    if (!match) return;
    dbg((_a = this.plugin.settings.debugMode) != null ? _a : false, "\u2705 right-click pointerdown on link \u2014 preventDefault to block CM6 cursor move");
    event.preventDefault();
  }
  /**
   * touchstart capture：iOS 上 Live Preview 折叠链接的跳转发生在 Obsidian 内部的
   * pointerdown/mousedown handler 中，比 document capture 更早执行（widget 内部监听）。
   * 唯一能在它之前拦截的只有 touchstart（capture + passive:false）。
   *
   * 策略：
   * - 检测到手指落在链接上 → preventDefault 阻止后续所有默认行为（包括 Obsidian 跳转）
   * - 保存链接上下文，在 touchend 里直接打开 popover
   * - touchmove 超过阈值时取消标记（恢复滚动能力——但注意 preventDefault 已调用，
   *   所以本次 touch 序列的滚动无法恢复；这是为了阻止跳转的必要代价）
   */
  handleTouchStart(event) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    this.touchIntercepted = false;
    this.touchLinkContext = null;
    if (!this.plugin.settings.enabled) return;
    const triggerMethod = (_a = this.plugin.settings.triggerMethod) != null ? _a : "hover";
    if (triggerMethod !== "click") return;
    const touch = event.touches[0];
    if (!touch) return;
    const target = event.target;
    if (!(target instanceof HTMLElement)) return;
    if (target.matches("img") || target.closest("img") || target.closest(".image-embed")) return;
    const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian5.MarkdownView);
    if (!(markdownView == null ? void 0 : markdownView.file) || !markdownView.containerEl.contains(target)) return;
    const cmEditorEl = target.closest(".cm-editor");
    if (!(cmEditorEl instanceof HTMLElement)) return;
    const editorView = import_view2.EditorView.findFromDOM(cmEditorEl);
    if (!editorView) return;
    const rect = target.getBoundingClientRect();
    const x = touch.clientX || rect.left + rect.width / 2;
    const y = touch.clientY || rect.top + rect.height / 2;
    dbg((_b = this.plugin.settings.debugMode) != null ? _b : false, "touchstart", { x, y, targetTag: target.tagName, targetClass: target.className });
    const documentOffset = editorView.posAtCoords({ x, y }, false);
    if (documentOffset == null) {
      dbg((_c = this.plugin.settings.debugMode) != null ? _c : false, "touchstart \u23F9 posAtCoords null");
      return;
    }
    const position = markdownView.editor.offsetToPos(documentOffset);
    const lineText = markdownView.editor.getLine(position.line);
    const match = findLinkAtOffset(lineText, position.ch, this.plugin.settings);
    if (!match) {
      dbg((_d = this.plugin.settings.debugMode) != null ? _d : false, "touchstart \u23F9 no link match");
      return;
    }
    if ((_e = this.plugin.settings.edgeProtection) != null ? _e : true) {
      const lineStartOffset = editorView.state.doc.line(position.line + 1).from;
      const matchStartOffset = lineStartOffset + match.start;
      const matchStart = editorView.coordsAtPos(matchStartOffset);
      const matchEnd = editorView.coordsAtPos(lineStartOffset + match.end);
      if (matchStart && matchEnd) {
        if (!this.isPointInsideLinkRect(x, y, documentOffset, matchStartOffset, lineStartOffset + match.end, matchStart, matchEnd)) {
          dbg((_f = this.plugin.settings.debugMode) != null ? _f : false, "touchstart \u23F9 edge protection rejected");
          return;
        }
      }
    }
    const excludeMode = (_g = this.plugin.settings.excludeMode) != null ? _g : "disabled";
    if (excludeMode === "click" || excludeMode === "all") {
      const keywords = parseExcludeKeywords(this.plugin.settings.excludeKeywords);
      if (matchesExcludeKeyword(match.destination, keywords)) {
        dbg((_h = this.plugin.settings.debugMode) != null ? _h : false, "touchstart \u23F9 excluded by keyword");
        return;
      }
    }
    this.touchStartX = x;
    this.touchStartY = y;
    this.touchIntercepted = true;
    this.touchLinkContext = { match, position, markdownView, target };
    dbg((_i = this.plugin.settings.debugMode) != null ? _i : false, "touchstart \u2705 preventDefault \u2014 blocking Obsidian navigation, will open popover on touchend");
    event.preventDefault();
    event.stopPropagation();
  }
  /**
   * touchmove：如果手指移动超过阈值（拖动手势），取消本次 touch 拦截。
   * 注意：由于 touchstart 已经 preventDefault，本次 touch 序列的滚动无法恢复，
   * 但标记取消后 touchend 不会打开 popover。
   */
  handleTouchMove(event) {
    var _a;
    if (!this.touchIntercepted) return;
    const touch = event.touches[0];
    if (!touch) return;
    const dx = Math.abs(touch.clientX - this.touchStartX);
    const dy = Math.abs(touch.clientY - this.touchStartY);
    const DRAG_THRESHOLD = 10;
    if (dx > DRAG_THRESHOLD || dy > DRAG_THRESHOLD) {
      dbg((_a = this.plugin.settings.debugMode) != null ? _a : false, "touchmove \u23F9 drag detected, cancelling intercept", { dx, dy });
      this.touchIntercepted = false;
      this.touchLinkContext = null;
    }
  }
  /**
   * touchend：touchstart 确认链接 + 未被 touchmove 取消 → 打开 popover。
   */
  handleTouchEnd(_event) {
    var _a;
    if (!this.touchIntercepted || !this.touchLinkContext) {
      this.touchIntercepted = false;
      this.touchLinkContext = null;
      return;
    }
    const { match, position, markdownView, target } = this.touchLinkContext;
    this.touchIntercepted = false;
    this.touchLinkContext = null;
    if (!match || !markdownView.file) return;
    const editorMatch = withEditorRange(match, position.line, markdownView.file.path);
    dbg((_a = this.plugin.settings.debugMode) != null ? _a : false, "touchend \u2705 opening popover for:", match.destination);
    if (!target.isConnected) {
      new import_obsidian5.Notice(this.plugin.t("noticeClickedLinkMissing"));
      return;
    }
    this.linkEditManager.show(editorMatch, target, target);
  }
  handleClick(event) {
    var _a;
    dbg((_a = this.plugin.settings.debugMode) != null ? _a : false, "click", { button: event.button, clientX: event.clientX, clientY: event.clientY, defaultPrevented: event.defaultPrevented, pointerDownIntercepted: this.pointerDownIntercepted });
    const skipDefaultPreventedCheck = this.pointerDownIntercepted;
    this.pointerDownIntercepted = false;
    return this.handlePointerTriggerEvent(event, "click", skipDefaultPreventedCheck);
  }
  handleContextMenu(event) {
    return this.handlePointerTriggerEvent(event, "contextmenu", false);
  }
  handleMouseMove(event) {
    var _a, _b;
    const triggerMethod = (_a = this.plugin.settings.triggerMethod) != null ? _a : "hover";
    if (triggerMethod !== "hover") return;
    if (!this.plugin.settings.enabled) return;
    const modifier = (_b = this.plugin.settings.triggerModifier) != null ? _b : "none";
    const hasAnyModifier = event.ctrlKey || event.metaKey || event.shiftKey || event.altKey;
    const modifierHeld = modifier === "none" ? !hasAnyModifier : modifier === "ctrl" && (event.ctrlKey || event.metaKey) || modifier === "shift" && event.shiftKey || modifier === "alt" && event.altKey;
    if (!modifierHeld) {
      this.clearHoverShowTimer();
      if (this.hoveredLinkKey !== null) {
        this.hoveredLinkKey = null;
        if (this.linkEditManager.isOpen()) {
          this.scheduleHoverHide();
        }
      }
      return;
    }
    this.processHover(event);
  }
  /**
   * hover 模式下鼠标进入 popover 区域时取消关闭计时
   */
  cancelHoverHide() {
    this.clearHoverHideTimer();
  }
  /**
   * hover 模式下鼠标离开 popover 区域时启动关闭计时
   */
  scheduleHoverHide() {
    if (this.linkEditManager.isUserInteracting()) {
      return;
    }
    this.clearHoverHideTimer();
    const delay = HOVER_LEAVE_DELAY;
    this.hoverHideTimer = window.setTimeout(() => {
      if (this.linkEditManager.isUserInteracting()) {
        return;
      }
      this.linkEditManager.close();
      this.hoveredLinkKey = null;
    }, delay);
  }
  destroy() {
    this.clearHoverShowTimer();
    this.clearHoverHideTimer();
  }
  async handlePointerTriggerEvent(event, eventType, skipDefaultPreventedCheck) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z, _A, _B, _C, _D, _E, _F;
    if (!this.plugin.settings.enabled) {
      dbg((_a = this.plugin.settings.debugMode) != null ? _a : false, "handlePointerTriggerEvent \u23F9 disabled");
      return;
    }
    if (!skipDefaultPreventedCheck && event.defaultPrevented) {
      dbg((_b = this.plugin.settings.debugMode) != null ? _b : false, "handlePointerTriggerEvent \u23F9 defaultPrevented");
      return;
    }
    const triggerMethod = (_c = this.plugin.settings.triggerMethod) != null ? _c : "hover";
    const triggerModifier = (_d = this.plugin.settings.triggerModifier) != null ? _d : "none";
    dbg((_e = this.plugin.settings.debugMode) != null ? _e : false, "handlePointerTriggerEvent", { eventType, triggerMethod, triggerModifier, skipDefaultPreventedCheck });
    if (eventType === "click" && event.button === 0 && triggerMethod !== "click" && ((_f = this.plugin.settings.disableNativeClick) != null ? _f : false) && !event.ctrlKey && !event.metaKey && !event.shiftKey && !event.altKey) {
      if (this.isClickOnEditorLink(event)) {
        dbg((_g = this.plugin.settings.debugMode) != null ? _g : false, "\u23F9 disableNativeClick intercepted");
        event.preventDefault();
        event.stopPropagation();
        return;
      }
    }
    if (triggerMethod === "hover") {
      dbg((_h = this.plugin.settings.debugMode) != null ? _h : false, "\u23F9 hover mode, skip click handling");
      return;
    }
    if (triggerMethod === "click") {
      if (eventType !== "click" || event.button !== 0) {
        dbg((_i = this.plugin.settings.debugMode) != null ? _i : false, "\u23F9 click mode but event mismatch", { eventType, button: event.button });
        return;
      }
    }
    if (triggerMethod === "right-click" && eventType !== "contextmenu") {
      dbg((_j = this.plugin.settings.debugMode) != null ? _j : false, "\u23F9 right-click mode but not contextmenu");
      return;
    }
    const target = event.target;
    if (!(target instanceof HTMLElement)) {
      dbg((_k = this.plugin.settings.debugMode) != null ? _k : false, "\u23F9 target not HTMLElement");
      return;
    }
    if (target.matches("img") || target.closest("img") || target.closest(".image-embed")) {
      dbg((_l = this.plugin.settings.debugMode) != null ? _l : false, "\u23F9 image target");
      return;
    }
    const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian5.MarkdownView);
    if (!(markdownView == null ? void 0 : markdownView.file)) {
      dbg((_m = this.plugin.settings.debugMode) != null ? _m : false, "\u23F9 no active MarkdownView/file");
      return;
    }
    if (!markdownView.containerEl.contains(target)) {
      dbg((_n = this.plugin.settings.debugMode) != null ? _n : false, "\u23F9 target not inside MarkdownView");
      return;
    }
    const cmEditorEl = target.closest(".cm-editor");
    if (!(cmEditorEl instanceof HTMLElement)) {
      dbg((_o = this.plugin.settings.debugMode) != null ? _o : false, "\u23F9 no .cm-editor ancestor");
      return;
    }
    const editorView = import_view2.EditorView.findFromDOM(cmEditorEl);
    if (!editorView) {
      dbg((_p = this.plugin.settings.debugMode) != null ? _p : false, "\u23F9 EditorView.findFromDOM returned null");
      return;
    }
    const documentOffset = editorView.posAtCoords({ x: event.clientX, y: event.clientY }, false);
    dbg((_q = this.plugin.settings.debugMode) != null ? _q : false, "click posAtCoords:", { clientX: event.clientX, clientY: event.clientY, documentOffset });
    if (documentOffset == null) {
      dbg((_r = this.plugin.settings.debugMode) != null ? _r : false, "\u23F9 posAtCoords returned null");
      return;
    }
    const position = markdownView.editor.offsetToPos(documentOffset);
    for (const sel of editorView.state.selection.ranges) {
      if (documentOffset >= sel.from && documentOffset < sel.to) {
        dbg((_s = this.plugin.settings.debugMode) != null ? _s : false, "\u23F9 offset is inside selection range");
        return;
      }
    }
    const lineText = markdownView.editor.getLine(position.line);
    dbg((_t = this.plugin.settings.debugMode) != null ? _t : false, "line", position.line, "ch", position.ch, JSON.stringify(lineText));
    const match = findLinkAtOffset(lineText, position.ch, this.plugin.settings);
    dbg((_u = this.plugin.settings.debugMode) != null ? _u : false, "findLinkAtOffset result:", match);
    if (!match) {
      const embedResult = this.resolveEmbedBlockLink(target, editorView, markdownView.file.path);
      if (!embedResult) return;
      const { editorMatch: editorMatch2, anchorEl } = embedResult;
      const exMode = (_v = this.plugin.settings.excludeMode) != null ? _v : "disabled";
      if (exMode === "click" || exMode === "all") {
        const kw = parseExcludeKeywords(this.plugin.settings.excludeKeywords);
        if (matchesExcludeKeyword(editorMatch2.destination, kw)) return;
      }
      if (triggerModifier === "ctrl") {
        if (!(event.ctrlKey || event.metaKey)) return;
      } else if (triggerModifier === "shift") {
        if (!event.shiftKey) return;
      } else if (triggerModifier === "alt") {
        if (!event.altKey) return;
      }
      event.preventDefault();
      event.stopPropagation();
      if (triggerMethod === "click" && triggerModifier === "none" && (event.ctrlKey || event.metaKey)) {
        const values = normalizeEditableValues(editorMatch2, editorMatch2.displayText, editorMatch2.destination);
        await openLink(this.plugin.app, editorMatch2, values, this.plugin.settings);
        return;
      }
      const embedVisibleEndCh = computeVisibleEndCh(editorMatch2);
      const referenceEl2 = triggerMethod === "right-click" ? this.createVirtualReferenceForRange(editorView, editorMatch2.range.from.line, editorMatch2.range.from.ch, embedVisibleEndCh, event.clientX, event.clientY) : anchorEl;
      this.linkEditManager.show(editorMatch2, referenceEl2, anchorEl);
      return;
    }
    const excludeMode = (_w = this.plugin.settings.excludeMode) != null ? _w : "disabled";
    if (excludeMode === "click" || excludeMode === "all") {
      const keywords = parseExcludeKeywords(this.plugin.settings.excludeKeywords);
      if (matchesExcludeKeyword(match.destination, keywords)) {
        dbg((_x = this.plugin.settings.debugMode) != null ? _x : false, "\u23F9 excluded by keyword:", match.destination);
        return;
      }
    }
    const editorMatch = withEditorRange(match, position.line, markdownView.file.path);
    const lineStartOffset = editorView.state.doc.line(position.line + 1).from;
    const matchStartOffset = lineStartOffset + match.start;
    const matchStart = editorView.coordsAtPos(matchStartOffset);
    const matchEnd = editorView.coordsAtPos(lineStartOffset + match.end);
    dbg((_y = this.plugin.settings.debugMode) != null ? _y : false, "click edgeProtection check:", { matchStart, matchEnd, clientX: event.clientX, clientY: event.clientY });
    if (((_z = this.plugin.settings.edgeProtection) != null ? _z : true) && matchStart && matchEnd) {
      const inside = this.isPointInsideLinkRect(event.clientX, event.clientY, documentOffset, matchStartOffset, lineStartOffset + match.end, matchStart, matchEnd);
      dbg((_A = this.plugin.settings.debugMode) != null ? _A : false, "isPointInsideLinkRect:", inside);
      if (!inside) {
        dbg((_B = this.plugin.settings.debugMode) != null ? _B : false, "\u23F9 edge protection rejected");
        return;
      }
    }
    if (triggerModifier === "ctrl") {
      if (!(event.ctrlKey || event.metaKey)) {
        dbg((_C = this.plugin.settings.debugMode) != null ? _C : false, "\u23F9 ctrl modifier not held");
        return;
      }
    } else if (triggerModifier === "shift") {
      if (!event.shiftKey) {
        dbg((_D = this.plugin.settings.debugMode) != null ? _D : false, "\u23F9 shift modifier not held");
        return;
      }
    } else if (triggerModifier === "alt") {
      if (!event.altKey) {
        dbg((_E = this.plugin.settings.debugMode) != null ? _E : false, "\u23F9 alt modifier not held");
        return;
      }
    }
    dbg((_F = this.plugin.settings.debugMode) != null ? _F : false, "\u2705 preventDefault + show popover, destination:", match.destination);
    event.preventDefault();
    event.stopPropagation();
    if (triggerMethod === "click" && triggerModifier === "none") {
      if (event.ctrlKey || event.metaKey) {
        const values = normalizeEditableValues(editorMatch, editorMatch.displayText, editorMatch.destination);
        await openLink(this.plugin.app, editorMatch, values, this.plugin.settings);
        return;
      }
    }
    if (!target.isConnected) {
      new import_obsidian5.Notice(this.plugin.t("noticeClickedLinkMissing"));
      return;
    }
    const virtualEndOffset = computeVisibleEndOffset(match, lineStartOffset);
    const referenceEl = triggerMethod === "right-click" ? this.createVirtualReference(matchStartOffset, virtualEndOffset, editorView, event.clientX, event.clientY) : this.resolveReferenceElement(event.clientX, event.clientY, target, markdownView);
    this.linkEditManager.show(editorMatch, referenceEl, target);
  }
  resolveReferenceElement(x, y, fallback, markdownView) {
    const pointEl = fallback.doc.elementFromPoint(x, y);
    if (pointEl instanceof HTMLElement && markdownView.containerEl.contains(pointEl)) {
      return pointEl;
    }
    return fallback;
  }
  /**
   * 判断点 (x, y) 是否在链接的可视矩形内。
   * 支持跨行（软换行）链接，以及 RTL 文字方向。
   *
   * RTL 检测策略：直接比较 matchStart.left 与 matchEnd.left。
   * 对于 RTL 文字，起始字符（逻辑上靠前）在视觉上位于右侧，
   * 因此 matchStart.left > matchEnd.left 表示 RTL。
   * 这个方法不依赖 DOM dir 属性，能正确处理以中性/弱字符开头的行
   * （这类行 CM6 不设置 dir="rtl" 但内容本身是 RTL 的情况）。
   *
   * - 单行链接：检查 min/max 水平边界
   * - 跨行链接：首行检查起始端边界，末行检查结束端边界，中间行整行命中
   */
  isPointInsideLinkRect(x, y, documentOffset, matchStartOffset, matchEndOffset, matchStart, matchEnd) {
    const BUFFER = 4;
    if (y < matchStart.top || y > matchEnd.bottom) return false;
    const isMultiLine = matchEnd.top > matchStart.bottom;
    const linkLeft = Math.min(matchStart.left, matchEnd.left);
    const linkRight = Math.max(matchStart.right, matchEnd.right);
    const isRtl = matchStart.left > matchEnd.left;
    if (!isMultiLine) {
      if (Math.abs(documentOffset - matchStartOffset) <= BUFFER) {
        if (isRtl ? x >= linkRight - BUFFER : x <= linkLeft + BUFFER) return false;
      }
      if (Math.abs(documentOffset - matchEndOffset) <= BUFFER) {
        if (isRtl ? x <= linkLeft + BUFFER : x >= linkRight - BUFFER) return false;
      }
      if (x < linkLeft - BUFFER || x > linkRight + BUFFER) return false;
      return true;
    }
    const onFirstLine = y <= matchStart.bottom;
    const onLastLine = y >= matchEnd.top;
    if (onFirstLine) {
      const startEdge = isRtl ? matchStart.right : matchStart.left;
      if (Math.abs(documentOffset - matchStartOffset) <= BUFFER) {
        if (isRtl ? x >= startEdge - BUFFER : x <= startEdge + BUFFER) return false;
      }
      return true;
    }
    if (onLastLine) {
      const endEdge = isRtl ? matchEnd.left : matchEnd.right;
      if (Math.abs(documentOffset - matchEndOffset) <= BUFFER) {
        if (isRtl ? x <= endEdge + BUFFER : x >= endEdge - BUFFER) return false;
      }
      if (isRtl ? x < matchEnd.left - BUFFER : x > matchEnd.right + BUFFER) return false;
      return true;
    }
    return true;
  }
  /**
   * 快速判断点击事件是否落在编辑器内的链接上。
   * 用于 disableNativeClick 拦截：只吞掉"点在链接上"的裸左键，不影响其他位置。
   */
  isClickOnEditorLink(event) {
    const target = event.target;
    if (!(target instanceof HTMLElement)) return false;
    const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian5.MarkdownView);
    if (!(markdownView == null ? void 0 : markdownView.file) || !markdownView.containerEl.contains(target)) return false;
    const cmEditorEl = target.closest(".cm-editor");
    if (!(cmEditorEl instanceof HTMLElement)) return false;
    const editorView = import_view2.EditorView.findFromDOM(cmEditorEl);
    if (!editorView) return false;
    const documentOffset = editorView.posAtCoords({ x: event.clientX, y: event.clientY }, false);
    if (documentOffset == null) return false;
    const position = markdownView.editor.offsetToPos(documentOffset);
    const lineText = markdownView.editor.getLine(position.line);
    return !!findLinkAtOffset(lineText, position.ch, this.plugin.settings);
  }
  createVirtualReferenceForRange(editorView, line, fromCh, toCh, fallbackX, fallbackY) {
    const lineFrom = editorView.state.doc.line(line + 1).from;
    return this.createVirtualReference(lineFrom + fromCh, lineFrom + toCh, editorView, fallbackX, fallbackY);
  }
  createVirtualReference(from, to, editorView, fallbackX, fallbackY) {
    const fallbackRect = fallbackX != null && fallbackY != null ? new DOMRect(fallbackX, fallbackY, 1, 1) : new DOMRect(0, 0, 1, 1);
    let lastRect = fallbackRect;
    return {
      contextElement: editorView.dom,
      getBoundingClientRect: () => {
        const start2 = editorView.coordsAtPos(from);
        const end2 = editorView.coordsAtPos(to);
        if (start2 && end2) {
          const rectLeft = Math.min(start2.left, end2.left);
          const rectRight = Math.max(start2.right, end2.right);
          lastRect = new DOMRect(rectLeft, start2.top, Math.max(1, rectRight - rectLeft), Math.max(1, start2.bottom - start2.top));
        } else if (start2) {
          lastRect = new DOMRect(start2.left, start2.top, 1, Math.max(1, start2.bottom - start2.top));
        }
        return lastRect;
      }
    };
  }
  // ── hover 逻辑 ──────────────────────────────────────────────────────────
  processHover(event) {
    const target = event.target;
    if (!(target instanceof HTMLElement)) {
      this.onHoverLeaveLink();
      return;
    }
    if (this.linkEditManager.isMouseOverPopover(target)) {
      this.clearHoverHideTimer();
      return;
    }
    if (target.matches("img") || target.closest("img") || target.closest(".image-embed")) {
      this.onHoverLeaveLink();
      return;
    }
    const result = this.resolveHoverLink(event, target);
    if (!result) {
      this.onHoverLeaveLink();
      return;
    }
    const { editorMatch, targetEl } = result;
    const linkKey = `${editorMatch.sourcePath}:${editorMatch.range.from.line}:${editorMatch.range.from.ch}`;
    if (linkKey === this.hoveredLinkKey) {
      this.clearHoverHideTimer();
      return;
    }
    this.clearHoverShowTimer();
    this.clearHoverHideTimer();
    this.hoveredLinkKey = linkKey;
    this.hoverShowTimer = window.setTimeout(() => {
      this.hoverShowTimer = null;
      if (!targetEl.isConnected) return;
      this.linkEditManager.show(editorMatch, targetEl);
    }, HOVER_DELAY);
  }
  resolveHoverLink(event, target) {
    var _a, _b;
    const markdownView = this.plugin.app.workspace.getActiveViewOfType(import_obsidian5.MarkdownView);
    if (!(markdownView == null ? void 0 : markdownView.file) || !markdownView.containerEl.contains(target)) {
      return null;
    }
    const cmEditorEl = target.closest(".cm-editor");
    if (!(cmEditorEl instanceof HTMLElement)) {
      return null;
    }
    const editorView = import_view2.EditorView.findFromDOM(cmEditorEl);
    if (!editorView) {
      return null;
    }
    const documentOffset = editorView.posAtCoords({ x: event.clientX, y: event.clientY }, false);
    if (documentOffset == null) {
      return null;
    }
    const position = markdownView.editor.offsetToPos(documentOffset);
    const lineText = markdownView.editor.getLine(position.line);
    const match = findLinkAtOffset(lineText, position.ch, this.plugin.settings);
    if (!match) {
      const embedResult = this.resolveEmbedBlockLink(target, editorView, markdownView.file.path);
      if (!embedResult) return null;
      const { editorMatch: editorMatch2, anchorEl } = embedResult;
      const exMode = (_a = this.plugin.settings.excludeMode) != null ? _a : "disabled";
      if (exMode === "hover" || exMode === "all") {
        const kw = parseExcludeKeywords(this.plugin.settings.excludeKeywords);
        if (matchesExcludeKeyword(editorMatch2.destination, kw)) return null;
      }
      return { editorMatch: editorMatch2, targetEl: anchorEl };
    }
    const excludeMode = (_b = this.plugin.settings.excludeMode) != null ? _b : "disabled";
    if (excludeMode === "hover" || excludeMode === "all") {
      const keywords = parseExcludeKeywords(this.plugin.settings.excludeKeywords);
      if (matchesExcludeKeyword(match.destination, keywords)) {
        return null;
      }
    }
    const lineStartOffset = editorView.state.doc.line(position.line + 1).from;
    const matchStartOffset = lineStartOffset + match.start;
    const matchEndOffset = lineStartOffset + match.end;
    let visibleEndOffset = matchEndOffset;
    if (match.type === "markdown" || match.type === "imageMarkdown") {
      const closeBracket = match.originalText.indexOf("]");
      if (closeBracket >= 0) {
        visibleEndOffset = lineStartOffset + match.start + closeBracket + 1;
      }
    }
    const matchStart = editorView.coordsAtPos(matchStartOffset);
    const matchEnd = editorView.coordsAtPos(visibleEndOffset);
    if (matchStart && matchEnd) {
      if (event.clientY < matchStart.top || event.clientY > matchStart.bottom) {
        return null;
      }
      const linkLeft = Math.min(matchStart.left, matchEnd.left);
      const linkRight = Math.max(matchStart.right, matchEnd.right);
      if (event.clientX < linkLeft || event.clientX > linkRight) {
        return null;
      }
    }
    const editorMatch = withEditorRange(match, position.line, markdownView.file.path);
    return { editorMatch, targetEl: target };
  }
  onHoverLeaveLink() {
    this.clearHoverShowTimer();
    this.hoveredLinkKey = null;
    if (this.linkEditManager.isOpen()) {
      this.scheduleHoverHide();
    }
  }
  clearHoverShowTimer() {
    if (this.hoverShowTimer !== null) {
      window.clearTimeout(this.hoverShowTimer);
      this.hoverShowTimer = null;
    }
  }
  clearHoverHideTimer() {
    if (this.hoverHideTimer !== null) {
      window.clearTimeout(this.hoverHideTimer);
      this.hoverHideTimer = null;
    }
  }
  // ── embed-block fallback ───────────────────────────────────────────────
  /**
   * 当标准的 posAtCoords → findLinkAtOffset 路径无法命中时（例如 callout 等
   * cm-embed-block 内的渲染链接），从 DOM `<a>` 元素反向查找源文本中的链接。
   *
   * 返回 null 表示不适用或未找到。
   */
  resolveEmbedBlockLink(target, editorView, sourcePath) {
    var _a, _b, _c, _d;
    const anchor = (_a = target.closest("a")) != null ? _a : target.matches("a") ? target : null;
    if (!anchor) return null;
    const embedBlock = anchor.closest(".cm-embed-block");
    if (!embedBlock || !embedBlock.instanceOf(HTMLElement)) return null;
    const href = (_b = anchor.getAttribute("data-href")) != null ? _b : anchor.getAttribute("href");
    if (!href) return null;
    let blockOffset;
    try {
      blockOffset = editorView.posAtDOM(embedBlock);
    } catch (e) {
      return null;
    }
    const doc = editorView.state.doc;
    const blockLine = doc.lineAt(blockOffset);
    const endOffset = Math.min(blockOffset + ((_d = (_c = embedBlock.textContent) == null ? void 0 : _c.length) != null ? _d : 0) * 4, doc.length);
    const endLine = doc.lineAt(endOffset).number;
    for (let lineNum = blockLine.number; lineNum <= endLine; lineNum++) {
      const line = doc.line(lineNum);
      const lineIndex = lineNum - 1;
      const match = findLinkByDestination(line.text, href, this.plugin.settings);
      if (match) {
        const editorMatch = withEditorRange(match, lineIndex, sourcePath);
        return { editorMatch, anchorEl: anchor };
      }
    }
    return null;
  }
};

// src/settingTab.ts
var import_obsidian6 = require("obsidian");
var BetterLinksSettingTab = class extends import_obsidian6.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
    this.icon = "link";
  }
  display() {
    const { containerEl } = this;
    const t2 = this.plugin.t.bind(this.plugin);
    containerEl.empty();
    containerEl.addClass("better-links-settings");
    new import_obsidian6.Setting(containerEl).setName(t2("settingsHeading")).setHeading();
    const behaviorGroup = new import_obsidian6.SettingGroup(containerEl).setHeading(t2("settingsGeneral")).addClass("better-links-settings-group");
    behaviorGroup.addSetting((setting) => {
      setting.setName(t2("settingsEnableEditorName")).setDesc(t2("settingsEnableEditorDesc")).addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.enabled).onChange(async (value) => {
          this.plugin.settings.enabled = value;
          await this.plugin.saveSettings();
        });
      });
    });
    behaviorGroup.addSetting((setting) => {
      setting.setName(t2("settingsEdgeProtectionName")).setDesc(t2("settingsEdgeProtectionDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.edgeProtection) != null ? _a : true).onChange(async (value) => {
          this.plugin.settings.edgeProtection = value;
          await this.plugin.saveSettings();
        });
      });
    });
    behaviorGroup.addSetting((setting) => {
      setting.setName(t2("settingsTriggerMethodName")).setDesc(t2("settingsTriggerMethodDesc")).addDropdown((dropdown) => {
        var _a;
        dropdown.addOption("hover", t2("settingsTriggerMethodHover")).addOption("click", t2("settingsTriggerMethodClick")).addOption("right-click", t2("settingsTriggerMethodRightClick")).setValue((_a = this.plugin.settings.triggerMethod) != null ? _a : "hover").onChange(async (value) => {
          this.plugin.settings.triggerMethod = value;
          await this.plugin.saveSettings();
          updateDisableNativeClickVisibility();
        });
      });
    });
    let disableNativeClickEl = null;
    const updateDisableNativeClickVisibility = () => {
      var _a;
      const isClickMode = ((_a = this.plugin.settings.triggerMethod) != null ? _a : "hover") === "click";
      disableNativeClickEl == null ? void 0 : disableNativeClickEl.toggleClass("is-hidden", isClickMode);
    };
    behaviorGroup.addSetting((setting) => {
      disableNativeClickEl = setting.settingEl;
      setting.setName(t2("settingsDisableNativeClickName")).setDesc(t2("settingsDisableNativeClickDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.disableNativeClick) != null ? _a : false).onChange(async (value) => {
          this.plugin.settings.disableNativeClick = value;
          await this.plugin.saveSettings();
        });
      });
    });
    updateDisableNativeClickVisibility();
    behaviorGroup.addSetting((setting) => {
      setting.setName(t2("settingsTriggerModifierName")).setDesc(t2("settingsTriggerModifierDesc")).addDropdown((dropdown) => {
        var _a;
        dropdown.addOption("none", t2("settingsTriggerModifierNone")).addOption("ctrl", t2("settingsTriggerModifierCtrl")).addOption("shift", t2("settingsTriggerModifierShift")).addOption("alt", t2("settingsTriggerModifierAlt")).setValue((_a = this.plugin.settings.triggerModifier) != null ? _a : "none").onChange(async (value) => {
          this.plugin.settings.triggerModifier = value;
          await this.plugin.saveSettings();
        });
      });
    });
    behaviorGroup.addSetting((setting) => {
      setting.setName(t2("settingsValidateInternalLinksName")).setDesc(t2("settingsValidateInternalLinksDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.validateInternalLinks) != null ? _a : true).onChange(async (value) => {
          this.plugin.settings.validateInternalLinks = value;
          await this.plugin.saveSettings();
        });
      });
    });
    behaviorGroup.addSetting((setting) => {
      setting.setName(t2("settingsAlwaysShowDisplayTextName")).setDesc(t2("settingsAlwaysShowDisplayTextDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.alwaysShowDisplayText) != null ? _a : false).onChange(async (value) => {
          this.plugin.settings.alwaysShowDisplayText = value;
          await this.plugin.saveSettings();
        });
      });
    });
    behaviorGroup.addSetting((setting) => {
      setting.setName(t2("settingsPadNewLinkWithSpacesName")).setDesc(t2("settingsPadNewLinkWithSpacesDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.padNewLinkWithSpaces) != null ? _a : true).onChange(async (value) => {
          this.plugin.settings.padNewLinkWithSpaces = value;
          await this.plugin.saveSettings();
        });
      });
    });
    const suggestGroup = new import_obsidian6.SettingGroup(containerEl).setHeading(t2("settingsLinkSuggestionsGroup")).addClass("better-links-settings-group");
    const suggestSubEls = [];
    const updateSuggestSubSettings = () => {
      var _a, _b, _c;
      const enabled = (_a = this.plugin.settings.enableLinkSuggestions) != null ? _a : true;
      const syncEnabled = (_b = this.plugin.settings.syncAlias) != null ? _b : false;
      const mode = (_c = this.plugin.settings.aliasSyncMode) != null ? _c : "heading-only";
      const showAliasSub = syncEnabled && mode !== "heading-only";
      for (const el of suggestSubEls) {
        el.toggleClass("is-hidden", !enabled);
      }
      if (enabled) {
        aliasModeSettingEl == null ? void 0 : aliasModeSettingEl.toggleClass("is-hidden", !syncEnabled);
        aliasSepSettingEl == null ? void 0 : aliasSepSettingEl.toggleClass("is-hidden", !showAliasSub);
        aliasTitlePropSettingEl == null ? void 0 : aliasTitlePropSettingEl.toggleClass("is-hidden", !showAliasSub);
      }
    };
    suggestGroup.addSetting((setting) => {
      setting.setName(t2("settingsEnableLinkSuggestionsName")).setDesc(t2("settingsEnableLinkSuggestionsDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.enableLinkSuggestions) != null ? _a : true).onChange(async (value) => {
          this.plugin.settings.enableLinkSuggestions = value;
          await this.plugin.saveSettings();
          updateSuggestSubSettings();
        });
      });
    });
    let aliasModeSettingEl = null;
    let aliasSepSettingEl = null;
    let aliasTitlePropSettingEl = null;
    suggestGroup.addSetting((setting) => {
      suggestSubEls.push(setting.settingEl);
      setting.setName(t2("settingsSyncAliasName")).setDesc(t2("settingsSyncAliasDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.syncAlias) != null ? _a : false).onChange(async (value) => {
          this.plugin.settings.syncAlias = value;
          await this.plugin.saveSettings();
          updateSuggestSubSettings();
        });
      });
    });
    suggestGroup.addSetting((setting) => {
      aliasModeSettingEl = setting.settingEl;
      suggestSubEls.push(setting.settingEl);
      setting.setName(t2("settingsAliasSyncModeName")).setDesc(t2("settingsAliasSyncModeDesc")).addDropdown((dropdown) => {
        var _a;
        dropdown.addOption("heading-only", t2("settingsAliasModeHeadingOnly")).addOption("filename-then-heading", t2("settingsAliasModeFileThenHeading")).addOption("heading-then-filename", t2("settingsAliasModeHeadingThenFile")).setValue((_a = this.plugin.settings.aliasSyncMode) != null ? _a : "heading-only").onChange(async (value) => {
          this.plugin.settings.aliasSyncMode = value;
          await this.plugin.saveSettings();
          updateSuggestSubSettings();
        });
      });
    });
    suggestGroup.addSetting((setting) => {
      aliasSepSettingEl = setting.settingEl;
      suggestSubEls.push(setting.settingEl);
      setting.setName(t2("settingsAliasSeparatorName")).setDesc(t2("settingsAliasSeparatorDesc")).addText((text) => {
        var _a;
        text.setValue((_a = this.plugin.settings.aliasSeparator) != null ? _a : " > ").onChange(async (value) => {
          this.plugin.settings.aliasSeparator = value;
          await this.plugin.saveSettings();
        });
      });
    });
    suggestGroup.addSetting((setting) => {
      aliasTitlePropSettingEl = setting.settingEl;
      suggestSubEls.push(setting.settingEl);
      setting.setName(t2("settingsAliasTitlePropertyName")).setDesc(t2("settingsAliasTitlePropertyDesc")).addText((text) => {
        var _a;
        text.setPlaceholder("Title").setValue((_a = this.plugin.settings.aliasTitleProperty) != null ? _a : "title").onChange(async (value) => {
          this.plugin.settings.aliasTitleProperty = value || "title";
          await this.plugin.saveSettings();
        });
      });
    });
    updateSuggestSubSettings();
    const linkTypeGroup = new import_obsidian6.SettingGroup(containerEl).setHeading(t2("settingsSupportedTypes")).addClass("better-links-settings-group");
    linkTypeGroup.addSetting((setting) => {
      setting.setName(t2("settingsWikiName")).setDesc(t2("settingsWikiDesc")).addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.enableWikiLinks).onChange(async (value) => {
          this.plugin.settings.enableWikiLinks = value;
          await this.plugin.saveSettings();
        });
      });
    });
    linkTypeGroup.addSetting((setting) => {
      setting.setName(t2("settingsMarkdownName")).setDesc(t2("settingsMarkdownDesc")).addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.enableMarkdownLinks).onChange(async (value) => {
          this.plugin.settings.enableMarkdownLinks = value;
          await this.plugin.saveSettings();
        });
      });
    });
    linkTypeGroup.addSetting((setting) => {
      setting.setName(t2("settingsUrlName")).setDesc(t2("settingsUrlDesc")).addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.enablePlainUrls).onChange(async (value) => {
          this.plugin.settings.enablePlainUrls = value;
          await this.plugin.saveSettings();
        });
      });
    });
    linkTypeGroup.addSetting((setting) => {
      setting.setName(t2("settingsImageName")).setDesc(t2("settingsImageDesc")).addToggle((toggle) => {
        toggle.setValue(this.plugin.settings.enableImages).onChange(async (value) => {
          this.plugin.settings.enableImages = value;
          await this.plugin.saveSettings();
        });
      });
    });
    const actionGroup = new import_obsidian6.SettingGroup(containerEl).setHeading(t2("settingsActions")).addClass("better-links-settings-group");
    actionGroup.addSetting((setting) => {
      setting.setName(t2("settingsOpenExternalName")).setDesc(t2("settingsOpenExternalDesc")).addDropdown((dropdown) => {
        dropdown.addOption("browser", t2("settingsOpenExternalBrowser")).addOption("obsidian", t2("settingsOpenExternalObsidian")).setValue(this.plugin.settings.externalLinkOpenMode).onChange(async (value) => {
          this.plugin.settings.externalLinkOpenMode = value;
          await this.plugin.saveSettings();
        });
      });
    });
    let smartSplitEl = null;
    const updateSmartSplitVisibility = () => {
      var _a;
      const mode = (_a = this.plugin.settings.internalLinkOpenMode) != null ? _a : "tab";
      const isSplit = mode === "split-horizontal" || mode === "split-vertical";
      smartSplitEl == null ? void 0 : smartSplitEl.toggleClass("is-hidden", !isSplit);
    };
    actionGroup.addSetting((setting) => {
      setting.setName(t2("settingsOpenInternalName")).setDesc(t2("settingsOpenInternalDesc")).addDropdown((dropdown) => {
        var _a;
        dropdown.addOption("current", t2("settingsOpenInternalCurrent")).addOption("tab", t2("settingsOpenInternalTab")).addOption("window", t2("settingsOpenInternalWindow")).addOption("split-horizontal", t2("settingsOpenInternalSplitH")).addOption("split-vertical", t2("settingsOpenInternalSplitV")).setValue((_a = this.plugin.settings.internalLinkOpenMode) != null ? _a : "tab").onChange(async (value) => {
          this.plugin.settings.internalLinkOpenMode = value;
          await this.plugin.saveSettings();
          updateSmartSplitVisibility();
        });
      });
    });
    actionGroup.addSetting((setting) => {
      smartSplitEl = setting.settingEl;
      setting.setName(t2("settingsSmartSplitName")).setDesc(t2("settingsSmartSplitDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.smartSplit) != null ? _a : true).onChange(async (value) => {
          this.plugin.settings.smartSplit = value;
          await this.plugin.saveSettings();
        });
      });
    });
    updateSmartSplitVisibility();
    actionGroup.addSetting((setting) => {
      setting.setName(t2("settingsDeleteBehaviorName")).setDesc(t2("settingsDeleteBehaviorDesc")).addDropdown((dropdown) => {
        dropdown.addOption("preserve-text", t2("settingsDeletePreserve")).addOption("remove-all", t2("settingsDeleteRemoveAll")).setValue(this.plugin.settings.deleteLinkBehavior).onChange(async (value) => {
          this.plugin.settings.deleteLinkBehavior = value;
          await this.plugin.saveSettings();
        });
      });
    });
    actionGroup.addSetting((setting) => {
      setting.setName(t2("settingsShowEmbedToggleName")).setDesc(t2("settingsShowEmbedToggleDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.showEmbedToggle) != null ? _a : false).onChange(async (value) => {
          this.plugin.settings.showEmbedToggle = value;
          await this.plugin.saveSettings();
        });
      });
    });
    const excludeGroup = new import_obsidian6.SettingGroup(containerEl).setHeading(t2("settingsExcludeGroup")).addClass("better-links-settings-group");
    let excludeKeywordsEl = null;
    const updateExcludeSubSettings = () => {
      var _a;
      const mode = (_a = this.plugin.settings.excludeMode) != null ? _a : "disabled";
      excludeKeywordsEl == null ? void 0 : excludeKeywordsEl.toggleClass("is-hidden", mode === "disabled");
    };
    excludeGroup.addSetting((setting) => {
      setting.setName(t2("settingsExcludeModeName")).setDesc(t2("settingsExcludeModeDesc")).addDropdown((dropdown) => {
        var _a;
        dropdown.addOption("disabled", t2("settingsExcludeModeDisabled")).addOption("hover", t2("settingsExcludeModeHover")).addOption("click", t2("settingsExcludeModeClick")).addOption("all", t2("settingsExcludeModeAll")).setValue((_a = this.plugin.settings.excludeMode) != null ? _a : "disabled").onChange(async (value) => {
          this.plugin.settings.excludeMode = value;
          await this.plugin.saveSettings();
          updateExcludeSubSettings();
        });
      });
    });
    excludeGroup.addSetting((setting) => {
      excludeKeywordsEl = setting.settingEl;
      setting.setName(t2("settingsExcludeKeywordsName")).setDesc(t2("settingsExcludeKeywordsDesc")).addTextArea((textArea) => {
        var _a;
        textArea.setPlaceholder(".base, .canvas").setValue((_a = this.plugin.settings.excludeKeywords) != null ? _a : ".base, .canvas").onChange(async (value) => {
          this.plugin.settings.excludeKeywords = value;
          await this.plugin.saveSettings();
        });
        textArea.inputEl.rows = 3;
        textArea.inputEl.cols = 30;
      });
    });
    updateExcludeSubSettings();
    const debugGroup = new import_obsidian6.SettingGroup(containerEl).setHeading(t2("settingsDebugGroup")).addClass("better-links-settings-group");
    debugGroup.addSetting((setting) => {
      setting.setName(t2("settingsDebugModeName")).setDesc(t2("settingsDebugModeDesc")).addToggle((toggle) => {
        var _a;
        toggle.setValue((_a = this.plugin.settings.debugMode) != null ? _a : false).onChange(async (value) => {
          this.plugin.settings.debugMode = value;
          await this.plugin.saveSettings();
        });
      });
    });
  }
};

// src/settings.ts
var DEFAULT_SETTINGS = {
  enabled: true,
  enableWikiLinks: true,
  enableMarkdownLinks: true,
  enablePlainUrls: true,
  enableImages: true,
  externalLinkOpenMode: "browser",
  deleteLinkBehavior: "preserve-text",
  edgeProtection: true,
  validateInternalLinks: true,
  enableLinkSuggestions: true,
  syncAlias: true,
  aliasSyncMode: "heading-only",
  aliasSeparator: " > ",
  aliasTitleProperty: "title",
  showEmbedToggle: true,
  triggerMethod: "hover",
  triggerModifier: "none",
  alwaysShowDisplayText: false,
  padNewLinkWithSpaces: true,
  disableNativeClick: false,
  excludeMode: "disabled",
  excludeKeywords: ".base, .canvas",
  internalLinkOpenMode: "tab",
  smartSplit: true,
  debugMode: false
};

// src/main.ts
var BetterLinksPlugin = class extends import_obsidian7.Plugin {
  constructor() {
    super(...arguments);
    this.translate = createTranslator(
      (0, import_obsidian7.getLanguage)()
    );
  }
  t(key) {
    return this.translate(key);
  }
  async onload() {
    await this.loadSettings();
    this.linkEditManager = new LinkEditManager(this);
    this.linkInterceptor = new LinkInterceptor(this, this.linkEditManager);
    this.addSettingTab(new BetterLinksSettingTab(this.app, this));
    this.addCommand({
      id: "close-link-editor",
      name: this.t("commandCloseLinkEditor"),
      checkCallback: (checking) => {
        const isOpen = this.linkEditManager.isOpen();
        if (checking) {
          return isOpen;
        }
        this.linkEditManager.close();
        return true;
      }
    });
    this.addCommand({
      id: "create-link-at-cursor",
      name: this.t("commandCreateLink"),
      checkCallback: (checking) => {
        const view = this.app.workspace.getActiveViewOfType(import_obsidian7.MarkdownView);
        const canRun = !!(view == null ? void 0 : view.file) && !this.linkEditManager.isOpen();
        if (checking) {
          return canRun;
        }
        this.linkEditManager.showAtCursor();
        return true;
      }
    });
    this.registerDocumentEvents(activeDocument);
    this.registerEvent(
      this.app.workspace.on("window-open", (_win, win) => {
        this.registerDocumentEvents(win.document);
      })
    );
    const popoverRoot = this.linkEditManager.popoverRootElement;
    this.registerDomEvent(popoverRoot, "mouseenter", () => {
      this.linkInterceptor.cancelHoverHide();
    });
    this.registerDomEvent(popoverRoot, "mouseleave", () => {
      var _a;
      if (((_a = this.settings.triggerMethod) != null ? _a : "hover") === "hover") {
        this.linkInterceptor.scheduleHoverHide();
      }
    });
    this.registerEvent(
      this.app.workspace.on("active-leaf-change", () => {
        this.linkEditManager.close();
      })
    );
    this.registerEvent(
      this.app.workspace.on("layout-change", () => {
        const activeView = this.app.workspace.getActiveViewOfType(import_obsidian7.MarkdownView);
        if (!activeView) {
          this.linkEditManager.close();
        }
      })
    );
  }
  onunload() {
    var _a, _b;
    (_a = this.linkEditManager) == null ? void 0 : _a.destroy();
    (_b = this.linkInterceptor) == null ? void 0 : _b.destroy();
  }
  /**
   * 将所有 document 级事件注册到指定 document 上。
   * 主窗口的 document 在 onload 调用，popout window 在 window-open 时调用。
   * registerDomEvent 会在 unload 时自动清理。
   */
  registerDocumentEvents(doc) {
    this.registerDomEvent(
      doc,
      "click",
      (event) => {
        void this.linkInterceptor.handleClick(event);
      },
      { capture: true }
    );
    this.registerDomEvent(
      doc,
      "pointerdown",
      (event) => {
        this.linkInterceptor.handlePointerDown(event);
      },
      { capture: true }
    );
    this.registerDomEvent(
      doc,
      "touchstart",
      (event) => {
        this.linkInterceptor.handleTouchStart(event);
      },
      { capture: true, passive: false }
    );
    this.registerDomEvent(
      doc,
      "touchmove",
      (event) => {
        this.linkInterceptor.handleTouchMove(event);
      },
      { capture: true, passive: true }
    );
    this.registerDomEvent(
      doc,
      "touchend",
      (event) => {
        this.linkInterceptor.handleTouchEnd(event);
      },
      { capture: true }
    );
    this.registerDomEvent(
      doc,
      "contextmenu",
      (event) => {
        void this.linkInterceptor.handleContextMenu(event);
      },
      { capture: true }
    );
    this.registerDomEvent(
      doc,
      "mousemove",
      (event) => {
        this.linkInterceptor.handleMouseMove(event);
      }
    );
  }
  async loadSettings() {
    this.settings = Object.assign({}, DEFAULT_SETTINGS, await this.loadData());
  }
  async saveSettings() {
    await this.saveData(this.settings);
  }
};

/* nosourcemap */